# CHECKPOINT — Walbox
Aggiornato: 2026-09-18 (Kitchen — AC7 multi-ordine stacked IMPLEMENTATO: `CustomerOrderStatus.jsx` è ora un container che mostra TUTTI gli ordini attivi del cliente impilati verticalmente, senza cap; ogni ordine è isolato nel nuovo componente `src/components/kitchen/CustomerOrderPanel.jsx` con proprio stato SumUp/reconciliation/scelta pagamento/CTA. AC1/AC3/AC4/AC5 preservati e riverificati. Vedi DONE per dettaglio, evidence e test. Lavoro NON committato, NON pushato.)
Fase: KITCHEN GO-LIVE 18/09 (invariato, vedi voce 2026-09-16 sotto) + Payment Lifecycle Sprint chiuso lo stesso giorno (2026-09-18). `main` e `integration/fantawalrus-kitchen-v1` allineati sullo stesso HEAD, deploy Vercel Production dal branch `main`. BEER SPRINT V1 Fase F (analytics) resta aperta e rimandata per decisione Eros. FantaWalrus, Jukebox/Spotify reale e Shuffle Night restano in pausa, invariati da V1.6.1 (2026-07-07).

---

## DONE
- **FINAL RELEASE 18/09 — Kitchen certificata e rilasciata su `main` (2026-09-16, approvazione Eros).** Chiusa la privacy client-side, ultimo residuo del track: `walbox_kitchen_orders_demo` era una chiave *role-agnostic* — su un device staff la sessione autenticata legge tutti gli ordini del locale e la hook li persisteva lì, quindi un browser passato da banco a cliente si portava dietro nickname, piatti, totali e note di ogni cliente. Il fix P0 `81174e9` impediva di mostrarli, non di scriverli sul dispositivo. Ora `useKitchenOrders({ scope })` separa le due superfici: staff/cassa/solo/TV restano su `walbox_kitchen_orders_demo` (invariato), il cliente usa `walbox_kitchen_my_orders`, filtrata per proprietà del device in lettura **e** in scrittura, senza fallback ai demo order; al primo mount cliente il device adotta solo gli ordini già propri e rimuove la cache del locale (pura cache, staff la ricostruisce dal fetch Supabase). Ownership registrata dentro `addOrder` solo in scope cliente: la cassa non rivendica mai ordini come "miei". Commit `0327133` (`src/hooks/useKitchenOrders.js`, `CustomerOrderStatus.jsx`, `CustomerKitchenMenu.jsx`, `CustomerRequest.jsx`, `tests/e2e/customer-kitchen-flow.spec.js`). Nessun cambio DB/RLS/schema/RPC/routing/deps.
  - **Verifiche pre-release:** build PASS; unit fanta 272/272; customer-kitchen-flow 41 passed / 1 skipped (incluso il nuovo scenario 29 di regressione anti-contaminazione); cassa C1–C10 PASS; guard staff `playwright.fsec2.config.js` 4/4 PASS; solo service / solo demo / training / refund contract PASS; lint invariato (zero nuovi error). QA manuale in browser reale: 390×844 cliente e 820×1180 staff.
  - **Fail pre-esistenti, NON introdotti da questo sprint** (riprodotti identici su HEAD pulito via `git stash`): `tests/unit/sumup-payment-reliability.test.mjs` (contratto retry checkout) e `kitchen-service-pressure.spec.js` test 2b (fixture non presente nel Supabase reale, la RPC cancel risponde `order_not_found`). Vedi OPEN ISSUES.
  - **Release gate:** `main` era antenato diretto di `integration/fantawalrus-kitchen-v1` → fast-forward pulito (`git merge --ff-only`), nessun merge commit, nessun force, nessun rebase. Report completo: `ai-ops/reports/client-side-privacy-storage-split-20260916.md` (gitignored, locale).
- **Kitchen (completato 2026-06-23, invariato da allora):** S1→S10d Supabase, Cleanup Sprint, V1-Competitive-Gap, V1-P6 flusso banco/ritiro. Playwright 17/17 PASS, build PASS.
- **Jukebox/Spotify reale (2026-06-24 → 2026-07-03, 39 commit):**
  - Auto-avanzamento coda basato su playback Spotify reale (fine naturale canzone, non solo skip manuale)
  - Fix mismatch progress TV durante auto-advance
  - Setup OAuth Spotify locale documentato
  - Skip to next helper per staff
  - Restore staff Spotify native queue flow
  - TV mostra now-playing reale da Spotify (non più mock)
  - Miglior reattività azioni staff dashboard
  - Subagent Claude Code per workflow prodotto (walbox-hardening, walbox-idea-lab, walbox-product-owner) + doc ricerca Shuffle Night
  - Implemented, but not yet verified with real E2E/QA flow — see OPEN ISSUES.
- CLAUDE.md aligned to current-state source of truth + agent routing — commit f077276, 2026-07-04.
- **QA serata walbox-qa-serata (2026-07-05): PASS WITH RISKS.** Flusso QR→richiesta→staff→coda verificato in locale con 2 client concorrenti su Supabase reale, nessun bug bloccante sul percorso principale. Non verificato: add-to-queue/now-playing con account Spotify Premium reale e device fisico. Bug trovato e fixato lo stesso giorno: approvazione di un brano senza `spotify_uri` (fallback MOCK_SONGS) non avvisava lo staff — vedi OPEN ISSUES per il fix.
- **ai-factory-runner V1 (2026-07-05):** primo router locale della AI Factory in `ai-ops/runner/` (Node ESM, zero dipendenze). Genera ticket/run log in `ai-ops/tickets/` con classificazione keyword, risk level, routing, scope, security reminders e prompt Claude Code pronto. Demo PASS con task `"Verifica TV Poster sync"`. V1 non esegue Claude, non chiama API, non modifica codice app e resta manuale nell'esecuzione.
- **Push su origin/main completato (2026-07-05):** 12 commit pendenti pushati con successo (`778e55e..ba55992`) dopo push readiness audit (PUSH OK WITH RISKS) e micro-patch pre-push (build PASS, rename file Fable, nota checkpoint stabilizzata). Branch `main` allineato a `origin/main`, working tree clean.
- **ai-factory-runner V1.1 (2026-07-05):** aggiunta lettura read-only di `CHECKPOINT.md` con snapshot sintetico (`STABLE` / `DONE` / `OPEN ISSUES` / `NEXT STEP`) incluso in ogni ticket generato nella nuova sezione 3. Zero scritture su `CHECKPOINT.md`, fallback sicuro se il file manca. Demo PASS su task nuovo e su `"Verifica TV Poster sync"`; retrocompatibile con V1.
- **Shuffle Night Jukebox pilot checklist S1 (2026-07-05):** creato `docs/PILOT_NIGHT_CHECKLIST_JUKEBOX.md` come checklist go/no-go dedicata a Jukebox / Shuffle Night. Copre QR cliente, richiesta brano, Staff Dashboard, Spotify reale, TV Poster, Supabase realtime, fallback staff, test manuale venue e template report finale. Commit locale `7a65193`.
- **Fix TV Poster preview/takeover bloccata dopo cambio traccia (2026-07-05):** risolta causa root in `LiveTvScreenWalrusPoster.jsx`: l'effect del takeover dipendeva da `currentRequest`, riferimento instabile con update Supabase Realtime, cancellando il timer di ritorno alla live screen. Patch con `currentRequestRef` e dipendenza solo da `remotePlayback?.spotify_track_uri`. `npm run build` PASS. Confermato manualmente da Eros.
- **ai-factory-runner V1.2-F (2026-07-05):** chiusi i 3 fix residui post-audit: `--dry-run` implementato senza scrittura ticket, precedenza executor corretta (`coding` / `coding-plan` / `design` vince su `qa` quando entrambe presenti), README e `task_classifier_rules.md` riallineati. Golden set esteso a 6 casi; nuovo Caso F conferma routing `coding+qa → walbox-dev`; 0 regressioni sui 5 casi precedenti. Commit locale `ebcc2c4`.
- **ai-factory-runner V1.3 (2026-07-05, commit `f39a2e7`):** aggiunti i campi `recommended_skill` + `prompt_mode` al ticket (nuova funzione pura `recommendSkillAndMode` in `run.js`, cascata a 11 regole: trigger lessicali diff/context/approval, poi condizioni su rischio/categorie/docRole/confidence). Puramente additivo: classificazione V1.2 (categorie/rischio/executor/confidence) invariata. Golden set esteso da 6 a 13 casi (A–M), 13/13 PASS in `--dry-run`. Template e doc (`README.md`, `task_classifier_rules.md`) allineati. Nota operativa: durante l'implementazione si sono verificati due revert esterni dei file del runner (probabile buffer editor stale in VS Code/Antigravity che sovrascriveva il disco al salvataggio) — riapplicati e riverificati prima del commit. Rischio risolto per questo round, ma da tenere a mente: prima di ogni futuro commit su questi file, ricontrollare `git diff --stat` subito prima di `git add`.
- **ai-factory-runner V1.4-A (completata 2026-07-06, committata localmente in `9e5496b`, non ancora pushata):** aggiunto `resolvePromptTemplate()` in `ai-ops/runner/run.js`. Il prompt Claude ora può essere scelto in base a `prompt_mode` (cerca `templates/prompts/claude_prompt_<mode>.md`), con fallback totale a `claude_prompt_template.md` se il template specifico non esiste. Nessun template per-modo ancora creato: output identico a V1.3 in assenza di template specifici. 13/13 golden case PASS in `--dry-run`. Prossimo step: V1.4-B, creare solo `claude_prompt_micro_fix.md` e `claude_prompt_phase_plan.md`.
- **ai-factory-runner V1.4-B (completata 2026-07-06, committata localmente in `9e5496b`, non ancora pushata):** creata la directory `ai-ops/runner/templates/prompts/` con i primi 2 template per-modo: `claude_prompt_micro_fix.md` e `claude_prompt_phase_plan.md`. `micro_fix_prompt` e `phase_plan_prompt` ora usano il template specifico; `checkpoint_prompt`, `handoff_prompt`, `review_prompt`, `approval_prompt` restano sul fallback base. 13/13 golden case PASS in `--dry-run`, zero regressioni sui metadata. Prossimo step: V1.4-C, creare i 4 template rimanenti e poi allineare README/rules.
- **ai-factory-runner V1.4-C1 (completata 2026-07-06, committata localmente in `9e5496b`, non ancora pushata):** creati `claude_prompt_review.md` e `claude_prompt_checkpoint.md` in `ai-ops/runner/templates/prompts/`. `review_prompt` e `checkpoint_prompt` ora usano il template specifico; `approval_prompt` e `handoff_prompt` restano sul fallback base. 13/13 golden case PASS in `--dry-run`, zero regressioni sui metadata. Prossimo step: V1.4-C2, creare `claude_prompt_approval.md` e `claude_prompt_handoff.md`.
- **ai-factory-runner V1.4-C2 (completata 2026-07-06, committata localmente in `9e5496b`, non ancora pushata):** creati `claude_prompt_approval.md` e `claude_prompt_handoff.md`. Tutti e 6 i `prompt_mode` ora hanno template specifico (micro_fix, phase_plan, review, checkpoint, approval, handoff) — nessun fallback base residuo nel golden set. 13/13 golden case PASS in `--dry-run`, zero regressioni sui metadata. V1.4 completa lato implementazione/template. Prossimo step: V1.4-C3, allineare `README.md` e `task_classifier_rules.md`.
- **ai-factory-runner V1.4-C3 (completata 2026-07-06, committata localmente in `9e5496b`, non ancora pushata):** allineati `ai-ops/runner/README.md` e `ai-ops/runner/rules/task_classifier_rules.md`. README aggiornato con sezione V1.4 (mapping dei 6 template e "quando si usa"), rimossa la nota obsoleta sul prompt unico; `task_classifier_rules.md` aggiornato con colonna `prompt_template` nel golden set e nuova sezione mapping — nessun valore golden modificato. 13/13 golden case PASS in `--dry-run`. V1.4 completa: resolver + 6 template + documentazione allineata. Prossimo step: review finale V1.4 e commit selettivo.
- **Push ai-factory-runner V1.4 su origin/main (2026-07-06):** pushati i commit `9e5496b` (template prompt_mode) e `26031d4` (checkpoint V1.4), range `e0462e0..26031d4`. Branch `main` allineato a `origin/main` per questo track. Restano non committate le modifiche pendenti su `CustomerEntry.jsx`/`CustomerRequest.jsx` (track separato, vedi NEXT STEP).
- **ai-factory-runner V1.4 — validazione manuale sul campo (2026-07-06):** eseguiti 6 dry-run (`--dry-run`, zero file scritti, zero ticket creati), uno per ciascun `prompt_mode`, con task raw realistici. Risultato: 3/6 pienamente corretti (micro_fix, approval, handoff), 3/6 con problema di **classificazione/routing a monte** (i template testuali restano tutti corretti, nessuno va riscritto):
  - PHASE PLAN: task "progetta il prossimo step... senza implementare" cade in `unclassified`→`handoff_prompt` invece di `phase_plan_prompt`, perché `coding-plan` in `CATEGORY_RULES` (`run.js`) riconosce solo "piano/plan/prepara piano/micro-task/spec", non verbi come "progetta"/"prossimo step".
  - REVIEW: task "verifica se V1.4 è pronta per push" ottiene giustamente `review_prompt`, ma executor `walbox-qa-serata` — pensato per il collaudo runtime della app Jukebox (QR/staff/TV) — non è adatto a validare un runner Node/CLI interno come ai-ops.
  - CHECKPOINT: task "aggiorna CHECKPOINT.md **senza toccare codice**" matcha comunque la keyword `codice` (presente in `coding`, `run.js:78`) per negazione non gestita, aggiungendo la categoria `coding` che nella cascata executor (`run.js:350` prima di `362`) vince su `checkpoint` → executor risultante `walbox-dev` invece di operatore doc/checkpoint.
  - Decisione presa: serve una micro-release V1.4.1 di fix routing (non nuovi template, non nuove feature) prima di progettare V1.5. Nessun file di prodotto toccato durante il test.
- **ai-factory-runner V1.4.1-A (completata 2026-07-07):** aggiunte le keyword `progetta` e `prossimo step` a `CATEGORY_RULES['coding-plan']` in `run.js`, e allineata la tabella keyword in `task_classifier_rules.md`. Aggiunto Caso N al golden set ("Progetta il prossimo step per migliorare il Runner dopo V1.4, senza implementare." → categorie `coding-plan`, executor `walbox-dev`, confidence high, `/phase-plan` + `phase_plan_prompt`). Golden set A–M: 13/13 PASS, zero regressioni. Caso N: PASS. Nessun ticket creato durante i test (`--dry-run`). Prossimo step: V1.4.1-B, gestire la negazione su "codice" per evitare falsi positivi tipo "senza toccare codice" (che oggi assegna comunque la categoria `coding`).
- **ai-factory-runner V1.4.1-B (completata 2026-07-07):** aggiunta negazione mirata sulla keyword `codice` (nuova funzione `isNegatedCodice` in `run.js`): un task tipo "aggiorna CHECKPOINT.md senza toccare codice" non assegna più la categoria `coding`. La negazione vale **solo** per `codice` dentro la categoria `coding` — nessuna generalizzazione ad altre keyword/categorie, e **nessuna modifica a `HIGH_RISK_KEYWORDS`/aree protette**: "non toccare Spotify" resta un segnale protetto a prescindere dalla negazione (scelta di sicurezza deliberata). Aggiunto Caso O al golden set ("Aggiorna CHECKPOINT.md dopo il push V1.4, senza toccare codice." → categorie `checkpoint`, executor `docs/checkpoint operator`, confidence high, `checkpoint_prompt`). Golden set A–N: 14/14 invariati PASS. Caso O: PASS. Golden set completo A–O: 15/15 PASS, zero regressioni. Nessun ticket creato durante i test (`--dry-run`). Prossimo step: V1.4.1-C, distinguere l'executor QA Runner/tooling interno da QA app/Jukebox (oggi sempre `walbox-qa-serata`).
- **ai-factory-runner V1.4.1-C (completata 2026-07-07):** aggiunta la distinzione tra QA su tooling interno (ai-ops/runner) e QA su app/Jukebox, prima sempre instradata su `walbox-qa-serata` a prescindere dal dominio. Nuova funzione pura `detectQaDomain(rawTask, categories)` in `run.js` (calcolata in `main()`, non dentro `recommendExecutor`), basata su due nuove keyword list (`TOOLING_QA_KEYWORDS`, `APP_QA_KEYWORDS`) più le categorie `tv`/`spotify`/`supabase`/`design` come segnale app: QA tooling puro → executor `Claude Code (verifica manuale ai-ops/runner, nessun subagente dedicato)` (nessun subagente dedicato oggi in CLAUDE.md §2); QA app/Jukebox → resta `walbox-qa-serata`, invariato; QA dominio misto (`mixed`) → executor invariato + nuovo warning esplicito, comportamento prudente di default. Puramente additivo: non tocca `CATEGORY_RULES`, `assessRisk`, `HIGH_RISK_KEYWORDS`, `EXPLICIT_AGENTS` né `recommendSkillAndMode`. Aggiornati `README.md` (sezione "Novità V1.4.1-C") e `task_classifier_rules.md` (nuova sezione dominio QA, cascata executor aggiornata, nuovo Caso P). Golden set A–O: 15/15 PASS invariati. Caso P ("Verifica se V1.4 è pronta per push su origin/main." → categorie `qa`, executor tooling, confidence high, `quality-gate-verifier`/`review_prompt`): PASS. Golden set completo A–P: 16/16 PASS, zero regressioni. Nessun ticket creato durante i test (`--dry-run`). V1.4.1 completa lato fix routing (A, B, C tutte fatte).
- **Validazione finale V1.4.1 pre-push + push su origin/main (2026-07-07):** eseguiti 5 dry-run mirati (nessun file modificato, nessun ticket creato) a conferma della cascata V1.4.1-A/B/C su casi realistici: coding-plan, checkpoint, qa tooling, qa app/Jukebox, qa dominio misto — tutti coerenti con l'atteso (l'unica nota: task qa+design finisce su `walbox-dev` per precedenza pre-esistente V1.2-F, comportamento by-design non regressione). Confermato che `081c8ec` = V1.4.1-C, contiene solo i 4 file attesi (`CHECKPOINT.md`, `ai-ops/runner/run.js`, `ai-ops/runner/rules/task_classifier_rules.md`, `ai-ops/runner/README.md`). Pushati i 3 commit locali `9ff6c0f` (V1.4.1-A), `bc67c6a` (V1.4.1-B), `081c8ec` (V1.4.1-C) su `origin/main` (`cfa5bd3..081c8ec`). Branch `main` allineato a `origin/main`, working tree pulito, stash customer/visual (`shuffle night customer copy rebrand`, `wip-tv-visual-before-shuffle-deploy`) intatto e non toccato. V1.4.1 chiusa. Prossimo step: progettare V1.5.
- **ai-factory-runner V1.5-A (2026-07-07):** CLI flags & output safety completata. RUNNER_VERSION unica
  (`V1.5-A`), drift V1.3/V1.4/V1.4.1 risolto, warning testuali visibili anche in
  `--dry-run`, nuovi flag `--show-prompt`, `--json`, `--help`/`-h`, rifiuto flag ignoti
  con exit code espliciti `0/1/2`. Fix post-diff-review: gli errori con `--json`
  (flag sconosciuto, task mancante, errore runtime gestibile) emettono JSON valido su stdout
  `{version, ok:false, error, exit_code}`, senza testo umano mischiato. Comportamento non-json
  invariato. Golden set A–P: 16/16 PASS, zero regressioni su classificazione/rischio/executor/skill/mode.
  File: `ai-ops/runner/run.js`, `ai-ops/runner/README.md`,
  `ai-ops/runner/rules/task_classifier_rules.md`.
- **ai-factory-runner V1.5-B (2026-07-07):** Project Profiles completati. Il Runner supporta
  `--project=<nome>` con default `walbox`. Creati `ai-ops/profiles/walbox.json` e
  `ai-ops/profiles/ai-factory.json`: i profili parametrizzano `code_dir`, `quality_gates`
  ed `explicit_agents`. Per `--project=ai-factory`, la categoria `qa` forza `qaDomain='tooling'`
  come intenzione esplicita dell'operatore; `detectQaDomain()` resta fallback/affinamento per gli altri casi.
  Classificazione testuale invariata su categorie/rischio/executor/skill/prompt_mode:
  golden set A–P 16/16 PASS. Nessuna modifica a `src/`, env, package, Supabase, Spotify o deploy.
- **ai-factory-runner V1.6 (SST + Run Pack, 2026-07-07, commit `1d9bc41`..`db16be6`):** aggiunti SST mode (Save Token Strict) e Run Pack V0 (`--write-run-pack` → `ai-ops/runs/<slug>/` con runner.json/context.md/claude_prompt.md/run_log.md). Git: pushato su origin/main.
- **ai-factory-runner V1.6.1 (silent + hardening, 2026-07-07, commit `ba55192`..`d674478`):** Terminal Silent Mode (CLAUDE.md §0.5), Silent Report Contract, known-files hardening. RUNNER_VERSION='V1.6.1' (run.js:141). Golden set A–P 16/16. Git: pushato su origin/main.
  File: `ai-ops/runner/run.js`, `ai-ops/runner/README.md`,
  `ai-ops/runner/rules/task_classifier_rules.md`, `ai-ops/profiles/walbox.json`,
  `ai-ops/profiles/ai-factory.json`.
- **Reality Sprint 01 — fix customer request realtime status sync (2026-07-08, commit `ad56461`):** `CustomerRequest.jsx` legge ora "Le mie richieste" da Supabase Realtime (`useRealtimeRequests()`), non più da mockData/localStorage — corretto il mismatch cliente/staff/TV su approved/playing/played. `npm run build` PASS, pushato su `origin/main`.
- **FantaWalrus — roster persistence, remote apply completato (2026-08-18):** applicate su `pcrqfdzipotprqtuemso` le migration `20260818120000_fanta_save_roster_rpc_v1.sql` + `20260818130000_fanta_roster_is_starter_v1.sql`, in ordine. `fanta_rosters.is_starter` ora presente sul remoto; RPC finale `save_fanta_roster_v1(uuid, jsonb)` unica (vecchia firma `(uuid, uuid[])` assente). 15 righe preesistenti in `fanta_rosters` preservate. Migration history remota riallineata (incluso il file locale ricostruito `20260806212722_0004_fanta_league_bootstrap_v1.sql`, contenuto identico al remoto, nessuna riscrittura). Test `node --test` 50/50 PASS, `npm run build` PASS. Verdict **ROSTER_PERSISTENCE_REMOTE_COMPLETE**. Gap dichiarato: nessun test RPC autenticato end-to-end reale contro Auth remoto (bloccato da email confirmation attiva, non un bug — da completare in un secondo momento con credenziali reali). Report: `ai-ops/reports/fantawalrus-roster-rpc-remote-apply-result.md`.
- **FantaWalrus — rebuild UI FantaTeamBuilder da Figma, in corso (WIP non committato):** piano a 7 fasi (F1-F7, vedi memoria `fantateam-figma-rebuild`) da tile-list a campo verticale, rif. Figma node 44:2 fileKey `XHEvlbwq6y7mkk6NRtEAxE`. Presenti e non committati: `src/fanta/components/team-builder/` (PlayerSlot, FantasyPitch, TeamHeader, BenchRow + test — F1-F4/F7), modifiche a `src/fanta/pages/FantaTeamBuilder.jsx`, `src/fanta/components/PlayerPicker.jsx`, `src/fanta/styles/fanta-system.css` (F5/F6 integrazione, in corso), più `tests/e2e/fanta-team-builder-mobile.spec.js` nuovo. Stato non verificato in questo checkpoint (nessun test/build mirato eseguito su questo WIP).
- **Micro-task stabilizzazione pre Extraction Audit (2026-08-25):** allineato questo CHECKPOINT.md allo stato FantaWalrus reale; aggiunti in `package.json` solo gli script `test:unit` (`node --test` su tutti i `*.test.js` rilevanti FantaWalrus: `src/fanta/**`, `src/lib/fanta*`, `supabase/migrations/*fanta*`, 30 file) e `test` (alias di `test:unit`). Nessun file `src/` toccato. `npm run test:unit`: 272/272 PASS. `npm run build`: PASS. Working tree lasciato invariato per il resto (WIP Figma rebuild sopra non toccato).
- **Kitchen — Solo Service Mode V2 finalizzato in locale (2026-08-27, gate visuale approvato da Eros):** nuova route `/kitchen/solo` (`src/App.jsx`, +2 righe: import + case) che monta `KitchenSoloService.jsx` (view CODA | FOCUS su tablet, single-focus su phone, next action unica per stato, allergeni prominenti, pagamento rapido con ritorno al focus precedente). Aggiunto `kitchenSoloPreviewFixtures.js` (fixture locali isolate, zero import Supabase) per DEV Preview Mode via `/kitchen/solo?preview=1`, che bypassa il guard staff senza toccare env/Auth reali. Test: `tests/e2e/kitchen-solo-service.spec.js` (nuovo, view V2 in isolamento) + append in `tests/e2e/f-sec-2-kitchen-guard.spec.js` (2 nuovi test P1/P2 su guard reale + preview bypass). Verificato in questa sessione: suite combinata 9 passed / 3 skipped / 2 failed — i 2 failed (T1, P1: redirect `/kitchen/login`) sono **pre-esistenti e attesi**, non causati da questo commit: la config Playwright di default imposta `VITE_E2E_BYPASS_STAFF_AUTH=true`, quindi il guard reale va verificato solo con `playwright.fsec2.config.js` (non eseguibile in locale per env Supabase mancanti — gap già noto, vedi report). Nessun file in area protetta toccato oltre alle 2 righe in `App.jsx` (via task dedicato). Report: `ai-ops/reports/kitchen-solo-service-mode-v2-audit.md`, `kitchen-solo-service-v2.md`, `kitchen-solo-service-v2-visual-polish.md`, `kitchen-solo-service-preview-mode.md`.
- **Kitchen — Solo Service Live Demo Harness (2026-08-27, Gate 2 approvato da Eros):** nuova route `/kitchen/solo-demo` (`src/App.jsx`, +2 righe: import + case, stesso pattern di `/kitchen/solo`) usabile anche in build Vercel production (a differenza del DEV Preview `?preview=1`, gate precedente giudicato insufficiente da Eros). Nuovi file isolati: `src/pages/kitchenSoloDemoFixtures.js` (hook `useDemoKitchenOrders()`, 6 fixture deterministiche in-memory — 2 PAGA, 2 DA FARE di cui una con allergene reale `item-002` glutine/uova/senape, 1 IN PREPARAZIONE, 1 PRONTO — con `simulateSyncError`/`retrySync`/`resetDemo`, zero localStorage/Supabase/polling) e `src/pages/KitchenSoloServiceDemo.jsx` (banner sticky "⚠ DEMO — NO LIVE DATA" + pulsanti SIMULA SYNC ✗ e RESET DEMO, quest'ultimo forza anche il remount della view per azzerare stato UI locale). Riusa `KitchenSoloServiceView` (ora `export`ata in `KitchenSoloService.jsx`, unica modifica a quel file) e `usePreviewKitchenMenu` esistenti: stessa UI, zero duplicazione. Verificato: build produzione (`npm run preview`) → banner/KPI/ciclo SYNC ✗→retry/reset OK, **0 richieste verso `*.supabase.co`**, 0 console error; `npm run build` PASS; `tests/e2e/kitchen-solo-service.spec.js` 11/11 PASS (nessuna regressione su `/kitchen/solo`); `f-sec-2-kitchen-guard.spec.js` 1 passed/2 failed/3 skipped, identico al baseline noto (non correlato); nuovo `tests/e2e/kitchen-solo-demo.spec.js` 6/6 PASS. Committato in locale, non pushato. Report: `ai-ops/reports/kitchen-solo-service-live-demo-harness.md`.
- **P0-A Kitchen Write Reliability — RESOLVED (2026-08-27, Gate 2 approvato da Eros):** `src/hooks/useKitchenOrders.js` non presenta più falsi successi su mutation staff (`updateOrderStatus`/`confirmPayment`/`cancelOrder`/`updateStaffNote`). Due failure mode chiusi: (1) write Supabase fallita in silenzio + poll 10s che riavvolgeva lo stato locale ottimistico → `supabaseUpdateOrder` ora ritorna `{ok,error}`, `runOrderSync`/`retrySync` marcano `syncStatus: 'pending'|'synced'|'error'` (+ `pendingWritesRef` esclude dal poll gli ordini con write pending/fallita), UI (`KitchenSoloService.jsx`/`.css`) mostra tag `SYNC ✗` in coda + banner con motivo e pulsante RIPROVA in focus; (2) `!session`/sessione anonima in una mutation staff ritornava `{ok:true, skipped:true}` trattato come `synced` (falso successo verde per token scaduto/revocato) → ora `{ok:false, error}`, stesso path di errore/retry del punto (1). Verificato: `supabaseUpdateOrder` è chiamata solo da mutation staff (mai da consumer guest `CustomerOrderStatus.jsx`/`CustomerKitchenMenu.jsx`, che usano solo `orders`/`addOrder`); DEV preview (`kitchenSoloPreviewFixtures.js`) isolata, zero import Supabase, non impattata. Test: `tests/e2e/kitchen-solo-service.spec.js` 11/11 PASS (8 preesistenti + 3 su write-failure/retry/poll-hold), nuovo `tests/unit/p0a-kitchen-sync.test.mjs` 3/3 PASS (no-session→error, sessione anonima→error, retry con sessione valida→synced; usa Vite in middleware mode, nessuna area protetta toccata). `npm run build` PASS. Regressione `f-sec-2-kitchen-guard.spec.js`: 1 passed/2 failed/3 skipped, identico al baseline noto (T1/P1 falliscono per `VITE_E2E_BYPASS_STAFF_AUTH=true` di default in locale, non correlato). Committato in locale, non pushato. Report: `ai-ops/reports/kitchen-p0a-silent-write-failure-fix.md`, `kitchen-p0a-no-session-fix.md` (gitignored, locali).
- **P0-B Kitchen Undo Window — PASS (2026-08-27, Gate 2 approvato da Eros):** protezione click accidentale su `KitchenSoloService.jsx`. Solo le transizioni PRONTO e CONSEGNATO offrono una finestra di undo di 9s (reverse sicure, nessun nuovo state/schema): toast sticky con testo ordine, pulsante ANNULLA esplicito e countdown; allo scadere auto-dismiss senza reverse automatico dello stato (mai un reverse silenzioso, solo click esplicito su ANNULLA). Un solo undo attivo alla volta. Nessun nuovo file: modifiche isolate a `src/pages/KitchenSoloService.jsx` (+state `undo`, `cancelUndo`, toast markup) e `.css` (stile toast `.kss-undo-*`). Test: `tests/e2e/kitchen-solo-service.spec.js` 15/15 PASS (12 preesistenti + 3 nuovi su toast/undo/scadenza; incluso il test P0-A su sync fallita che resta invariato). `npm run build` non ri-eseguito in questo passaggio (nessuna modifica a build config/dipendenze). Committato in locale, non pushato. Report: `ai-ops/reports/kitchen-p0b-undo-window-audit.md`.
- **Kitchen Fase 0 schema confirmed + baseline locale creata, non applicata (2026-08-28).** Report: `ai-ops/reports/kitchen-schema-baseline-v1-audit.md`.
- **Kitchen Payment Hub V1 — SumUp sandbox E2E reale confermato + fix lifecycle applicato al remoto (2026-08-28, Gate approvato da Eros):** contrariamente all'header "LOCAL BUILD ONLY — NOT APPLIED TO REMOTE" nelle migration `20260828100000`→`20260828100700`, schema/RPC (`kitchen_payments`, `kitchen_payment_webhook_events`, `kitchen_payment_confirm/fail/webhook_ingest`, alter `kitchen_orders`) sono **già live e funzionanti sul remoto** `pcrqfdzipotprqtuemso` (verificato con 2 pagamenti SumUp sandbox E2E reali, non solo audit statico). Bug reale trovato nel primo E2E: `kitchen_payment_confirm` settava `payment_status=paid`/`paid_at` ma non avanzava `kitchen_orders.status` da `pending_counter_payment`, bloccando l'ordine su "IN ATTESA PAGAMENTO" a pagamento già incassato. Fix: nuova migration `supabase/migrations/20260828100800_kitchen_payment_confirm_lifecycle_fix_v1.sql` (`CREATE OR REPLACE`, nessuna migration esistente toccata) — a conferma pagamento, se `status='pending_counter_payment'` avanza atomicamente a `'received'` (stessa transazione/lock riga `FOR UPDATE`), altrimenti lascia `status` invariato (mai regressioni su preparing/ready/delivered/cancelled); `kitchen_payment_fail` non tocca mai `status`. Testato su Postgres locale reale (Docker + `supabase db reset`, poi `ROLLBACK`, zero side-effect) su 6 scenari (pending→received, duplicate confirm idempotente, preparing+duplicate, ready/delivered/cancelled non regrediscono, fail non avanza, regression RPC/build) tutti PASS. Applicato al remoto **non** con `supabase db push` (bloccato hard-fail: 8 migration remote-only `20260827234726`→`20260827235012` senza contropartita locale, vedi OPEN ISSUES) ma con apply mirato via `supabase db query --linked --project-ref pcrqfdzipotprqtuemso -f <file>` (esegue solo l'SQL di quel file, nessun ledger/altre migration coinvolte). Riverificato con un secondo pagamento SumUp sandbox E2E reale post-fix: `status: pending_counter_payment → received`, `payment_status=paid`, `paid_at` valorizzato, e duplicate confirm manuale sullo stesso attempt → idempotente, `status` resta `received`, un solo record `payment_confirmed` in `kitchen_action_log`. Nessun commit/push/deploy eseguito.

- **Customer Kitchen — PRE-PUSH GATE (2026-09-04, branch `integration/fantawalrus-kitchen-v1`, 5 commit locali `ba41438`→`bc4c679`, NON pushati):** chiusi i fix customer-facing residui del rebuild Figma. `ddbfc0c` carica Barlow Condensed da Google Fonts (`index.html`) — tutte le sezioni Figma customer la dichiaravano ma non era mai stata richiesta, quindi cadevano in silenzio su Oswald; `88a309f` risolve i tap ingoiati dalla `.kitch-bottom-bar` fissa sulle CTA LO VOGLIO / FALLO PESANTE dell'ultima card Pesi Massimi (`scrollIntoView` a fine transizione + `scroll-margin-bottom`); `393c9a1` aggancia la navigazione HOME→CATEGORIE→LISTA alla history del browser (push `{view}` sullo stesso pathname `/kitchen` + listener `popstate`; i due back-button UI delegano a `history.back()`) — prima back browser e gesture uscivano da `/kitchen`, **`src/App.jsx` non toccato**; `f189a6a` ripara il banner upsell del drawer carrello, che renderizzava `<img src={kitchenCartUpsell.image}>` su un campo `image` rimosso dal WIP (immagine rotta a tutta larghezza) → ora usa il layout compatto icone+testo+prezzo che il CSS già conteneva inutilizzato (`.kitch-upsell-icons/-text/-price`), card 344×61px, "FALLO COMBO / Patatine + Birra / +€2,00"; `bc4c679` correzione copy ingredienti Combo CAVALLOOOO. Riconciliato `kitchenMockData.js`: **revertite 2 modifiche WIP stale** — `item-005` da `'bevande'` a `'birre'` (`MenuView.jsx:3` `CATEGORY_ORDER` non conosce `bevande` e `item-006` era rimasto `birre`: la Birra del Tricheco sarebbe uscita dal menu staff) e `kitchenCategoryPromos.bbq` di nuovo `null` con commento (`CustomerKitchenMenu.jsx:580` esclude esplicitamente `bbq` dalla promo hero → era dato morto). `CustomerKitchenMenu.css` ha diff netto **zero** vs `ba41438` (rimossa la regola WIP `.kitch-upsell-promo-img`, ora morta). QA reale eseguita in Chromium a 390×844: CTA Pesi Massimi real-click PASS (`elementFromPoint` restituisce il bottone, click reale → carrello €15,00), catena back browser LISTA→CATEGORIE→HOME→ENTRY PASS, back UI PASS, accordion single-open PASS, tutte 5 le categorie PASS (8/3/4/3/2 voci), 0 immagini rotte su 49 `<img>`, 0 overflow orizzontale, 0 console error applicativi. `npm run build` PASS **anche su worktree pulito al nuovo HEAD** (senza il WIP Sprint3B): i 5 commit non dipendono da file untracked. Regressione E2E misurata su due worktree isolati: `customer-kitchen-flow.spec.js` 22 passed / 4 failed a `ba41438` e **gli stessi** 22/4 a `bc4c679` → zero regressioni. Working tree lasciato deliberatamente dirty con il WIP Sprint3B/fulfillment/realtime, non inglobato. Verdetto **PRE_PUSH_BLOCKED** su 2 blocker preesistenti (vedi OPEN ISSUES), non su lavoro tecnico mancante. Report: `ai-ops/reports/customer-kitchen-pre-push-gate.md`.
- **Customer Kitchen — fresh review + fix back navigation (2026-09-04, commit `1f327e0`, HEAD finale del branch):** una review read-only in contesto separato sui 5 commit ha dato **PASS WITH RISKS**, confermando in autonomia zero coupling col WIP e zero regressioni, ma trovando **3 difetti reali introdotti da `393c9a1`**, tutti riprodotti e corretti in `1f327e0`: (1) rientrando su `/kitchen` da `/kitchen/status` il `replaceState({view:'home'})` al mount sovrascriveva lo state della entry ripristinata dal browser → il back portava a HOME invece che alla lista e il back successivo andava *avanti* in CATEGORIE; ora il mount **adotta** la entry ripristinata invece di sovrascriverla; (2) sulla schermata ORDINE RICEVUTO (renderizzata a prescindere da `view`) i back venivano consumati a vuoto e al terzo si usciva dal sito perdendo il codice ordine → ora il back esce dalla conferma, ed è controllato **prima** del ramo drawer perché l'ordine si invia dal drawer aperto e `cartOpen` è ancora true; (3) col drawer carrello aperto il back cambiava la schermata sottostante lasciando il drawer su → ora chiude solo il drawer e ripristina la entry consumata. Le entry portano anche `category`, così il back torna alla lista realmente in lettura e non a PANINI di default. Riverificato in Chromium 390×844: catena `LISTA(bbq) → drawer → back chiude drawer → ORDINE RICEVUTO → back da /kitchen/status → LISTA(bbq) → CATEGORIE → HOME`, monotona, nessun back a vuoto. `npm run build` PASS e E2E 22 passed / 4 failed **identici al baseline** su worktree pulito al nuovo HEAD. Rilievi della review lasciati deliberatamente aperti perché richiedono decisioni di Eros: allergeni `item-007` (`['glutine','pesce']` incoerenti con il copy nuovo — **campo allergeni NON toccato**, è una decisione di sicurezza alimentare), cambio nome prodotto `Upgrade Combo → Fallo Combo` entrato in `f189a6a` (impatto cross-surface su ticket cucina/banco e ordini storici), messaggio di `f189a6a` scritto rispetto al working tree sporco e non al parent (diff corretto, razionale no — storia non riscritta), asset orfano `aggiungi_e_risparmia_combo_digitale.png`, e il rilievo preesistente più impattante: **gli 8 panini hanno tutti `price: null` → categoria PANINI non ordinabile dal cliente** (i prezzi sono un input di business, non inventati).

- **Customer Kitchen — LONG RUN FINALE pre-push (2026-09-04, HEAD `09ea248`, 9 commit locali `ba41438`→`09ea248`, NON pushati): verdetto PRE_PUSH_BLOCKED.** Un solo commit creato in questa sessione, `09ea248`, esattamente 2 file (nuovo asset + 1 riga di riferimento): sostituita la foto SCAMORZA & CIPOLLE col watermark "Cicchetto 3" con quella pulita approvata da Eros. Nessun file Sprint3B/fulfillment/realtime staged. **Audit di separazione:** tutti gli 8 file modificati nel working tree importano da `src/lib/kitchenOrderContext.js` (untracked) — un solo intento estraneo; `CustomerKitchenMenu.jsx` e `kitchenMockData.js` non compaiono in quel diff, e nessuno dei 3 moduli WIP untracked è importato dai file del track Customer Menu: zero coupling, verificato in entrambe le direzioni. **QA reale 390×844** (Chromium, worktree isolato al solo HEAD committato, override di processo `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY=` senza toccare `.env.local`): journey ENTRY→HOME→CATEGORIE→5 categorie→expanded→cart→invio→status→back tutto PASS — asset nuovo servito 200 e renderizzato senza watermark in lista e in expanded, real click sulle CTA dell'ultima card Pesi Massimi (`elementFromPoint` → `pm-btn-want`/`pm-btn-heavy`, click → carrello €15,00), accordion single-open (1 solo body visibile h=439, gli altri 3 a h=0), back browser col drawer aperto che chiude solo il drawer, catena back monotona LISTA→CATEGORIE→HOME→ENTRY, back UI, rientro da `/kitchen/status` che adotta la entry ripristinata, ordine inviato con codice `W-1UH8F`; **0 immagini rotte** e **0 px di overflow** su tutte e 5 le categorie a 360/390/430 di larghezza; **0 console error** applicativi (i 9 in log sono di una tab stale su :5174). **Test:** `npm run build` PASS anche su worktree pulito (i commit non dipendono dal WIP untracked), `npm run test:unit` 272/272 PASS, E2E `customer-kitchen-flow.spec.js` **25 passed / 1 failed** (era 22/4). **Fresh review read-only in contesto separato: FAIL** — ha trovato un blocker che la QA di percorso non aveva intercettato (vedi PRE-PUSH BLOCKER 3), riprodotto e confermato in autonomia prima di accettarlo; ha invece giudicato corretto `09ea248` e verificato clean integrità dati, path immagini, 16 foto menu senza watermark, font Barlow Condensed e assenza di coupling col WIP. Rilievi should-fix aperti e verificati: allergeni **invisibili in tutte e 5 le categorie navigabili** (il blocco chip a `CustomerKitchenMenu.jsx:713-731` è nel ramo generico escluso a `:684`, e nessuna delle 5 section li renderizza — riguarda anche `pesce` su item-025), `ALLERGEN_LABEL` con chiave `noci` mentre i dati usano `frutta_secca`, `PesiMassimiSection.jsx:143,166` unica section senza guard `noPrice` (latente), zero copertura di test sulla back navigation, separatore decimale incoerente. Report: `ai-ops/reports/customer-kitchen-prepush-final.md`.

- **Customer Kitchen — VISUAL CLOSURE (2026-09-04, HEAD `87bd41f`, 7 commit locali `be7c031`→`87bd41f` sopra `4b30172`, NON pushati): verdetto FINAL_VISUAL_READY.** Chiusi tutti i P0/P1 visuali customer-facing; verdetto finale del `visual-reviewer` V1.1 read-only in worktree isolato: **SHIP-READY, P0 0 · P1 0 · P2 0 · ENV_BLOCKER 0**, coverage completa 8 surface × 390/430/1440, con approvazione esplicita. **Foto EXPANDED**: le 4 categorie con card espandibile (Panini, Cicchetti, Insalatone, Tartare) usano ora un pattern unico `aspect-ratio: 4/3` + `object-fit: cover` + `object-position: center` — prima erano box `height:192px` + `contain` su foto 1:1 1254×1254, cioè renderizzate 192×192 con bande `#fdf7f1` da 92–95px per lato a 390 e 620px a 1440. Il 4:3 (75% dell'altezza visibile, centrata) contiene per intero il soggetto di ogni foto, misurato con maschera luminanza+saturazione su tutte e 17 le foto; stato CLOSED invariato, confermato al pixel vs Figma `164:2`. Stesso fix su `.kh-card-photo` della Home. **Peso asset**: 29 file convertiti in WebP con qualità scelta per file cercando il PSNR ≥ 40 dB al peso minimo (misurato 40,0–41,6 dB, nessun ridimensionamento, alpha preservato, conversione con python3/Pillow di sistema, zero package npm aggiunti) — asset referenziati dal menu cliente **49,9 MB → 8,70 MB (−82,6%)**, `dist/assets/kitchen` **76,01 → 25,57 MB**; aggiornati solo i riferimenti immagine (27 in `kitchenMockData.js`, 1 in `CustomerKitchenMenu.jsx`). Esclusi di proposito `walrus-chef.png` (referenziato anche da `CustomerOrderStatus.jsx`, che ha WIP Sprint3B non committato) e `02_hero_combo_porcheria_seria.png` (superficie TV). **Altri fix**: `.kitch-page` è ora una colonna da 480px centrata con bottom bar e drawer allineati (a 1440 le card erano nastri da 1426px) — `width: 100%` è necessario perché è item di un flex container in colonna e `margin-inline:auto` da solo ne disattiva lo stretch; nome prodotto nel drawer carrello su una riga a 390/430 con target di tocco invariati 36×36; reset dell'`uppercase` ereditato dalla regola globale `button` sugli ingredienti delle card chiuse (`index.css` **non** toccato, è condiviso con Jukebox/Poster); sottotitolo di sezione in Barlow Condensed su una riga; tab attivo portato in vista via `scrollIntoView` mantenendo gap/padding approvati; cambio categoria da tab che fa `replaceState` (non push: è navigazione laterale) così `history.state` non resta stale e il reload ripristina la categoria giusta. **Test**: `npm run build` PASS anche su worktree pulito, `npm run test:unit` 272/272, E2E `customer-kitchen-flow.spec.js` **23 passed / 3 failed identico al baseline `4b30172` misurato in worktree separato → zero regressioni**. **Journey reale 390×844** su worktree pulito senza `.env.local` (nessuna scrittura sul Supabase reale): ENTRY→HOME→CATEGORIE→BRISKET→real click con `elementFromPoint`→carrello→invio→ORDINE RICEVUTO `W-P7EMT`→back reale→nuovo ordine possibile; 0 immagini rotte, 0 overflow, 0 errori console. Quattro giri di audit read-only, ognuno su worktree isolato sull'HEAD committato; due difetti introdotti da me e trovati dagli audit sono stati corretti prima di chiudere (il `max-width` senza `width:100%` che mandava `.kitch-page` a 424px sfondando il viewport a 390, e il gap tab 8px che azzerava la distanza fra le etichette lunghe). Report: `ai-ops/reports/customer-kitchen-final-visual-closure.md`, `customer-kitchen-final-visual-audit.md`.

- **Sprint 3B Kitchen — Code + Automated Tests baseline chiusa (2026-09-06, `integration/fantawalrus-kitchen-v1`, committato in locale, NON pushato). Stato: `CODE_READY_FOR_RUNTIME_CERTIFICATION: YES`, `SPRINT3B_FINAL_CERTIFIED: NO`.** Cleanup legacy tavoli/asporto completato: nessun riferimento applicativo residuo a `fulfillmentType`/`table`/`asporto` nel flusso Kitchen (verificato per grep); `/kitchen/entry` è ora un redirect di sola compatibilità a `/kitchen` (`replaceState`, niente form/tavolo); pill tavolo vuoti rimossi da `AlertView.jsx`/`StoricoView.jsx`/`KitchenOrdersView.jsx` (allergeni raggruppati per ordine, non più per tavolo), classe CSS morta `.ksd-row-table` rimossa, fixture demo/preview ripulite dal campo `table`. Pagamento banco allineato: bottone unico "PAGATO" splittato in `CONTANTI ✓` / `CARTA/POS ✓` (`CounterOrdersView.jsx`), KPI "DA PAGARE" rinominato "DA INCASSARE" (`KitchenSoloService.jsx`). Notifica audio nuovo ordine presente (`src/hooks/useKitchenAudio.js`, non ancora committato in questa baseline — vedi WORKTREE). Realtime publication + RPC Payment Hub (`kitchen_payment_record_counter`, `kitchen_payment_confirm/fail`) già live sul remoto, non ri-verificati in questa run (invariato da 2026-08-28/08-30). **Suite automatizzata, tutta verde:** `tests/e2e/kitchen-solo-service.spec.js` 17/17 PASS, `tests/e2e/customer-kitchen-flow.spec.js` 26/26 PASS, unit migration `20260901120000_kitchen_pilot_order_contract_v1.test.js` 5/5 PASS, unit `tests/unit/sprint3b-kitchen-realtime.test.mjs` 2/2 PASS, `npm run build` PASS. Il redesign promo/menu cliente resta **chiuso separatamente** nel commit `80b0c46` (non riaperto, non toccato in questa baseline). **Prossimo gate: certificazione runtime multi-device reale** (staff + cliente concorrenti su Supabase reale, bloccata oggi dal sandbox — richiede Eros da terminale reale). **Resta OPEN e separato** (vedi OPEN ISSUES): il disallineamento del migration ledger Kitchen Payment Hub (blocca `supabase db push`, non blocca l'app) e il bridge Jukebox `CustomerOrderStatus.jsx` che naviga a `/request?table=` con fallback hardcoded `'7'` — cross-domain Kitchen/Jukebox, **non va modificato senza una decisione dedicata**. Report: `ai-ops/reports/sprint3b-closure-run-fase-ab.md`, `ai-ops/reports/sprint3b-closure-run2-test-alignment.md`.

- **Sprint 3B Kitchen — FINAL INTEGRATION CLOSURE (2026-09-07, `integration/fantawalrus-kitchen-v1`, commit locale sopra `9a10612`, NON pushato).** Chiuso il WIP che restava dirty dopo la baseline del 2026-09-06. **`src/hooks/useKitchenAudio.js` committato: era untracked ma `KitchenSoloService.jsx` (già committato a `9a10612`) lo importa a riga 8, quindi HEAD da solo non era buildabile** — non era un file opzionale. Committato anche `tests/unit/sprint3b-kitchen-realtime.test.mjs` (2/2), verificato riga per riga contro `useKitchenOrders.js:77-88`: canale/tabella/filtro `venue_id=eq.walrus-main` e refresh canonico combaciano col codice corrente. **Decisione Payments (Eros):** preservato il comportamento refund esistente, **incluso il rimborso ledger-only per provider non-SumUp**; il gate `provider === 'sumup'` proposto in audit è stato **scartato** — `api/kitchen-sumup-refund.js:83` ha un ramo dedicato ai provider non-online e da Sprint 3B l'incasso al banco produce provider `cash`/`manual`, quindi quel gate avrebbe reso non rimborsabile dalla UI il flusso di pagamento principale. Di `PaymentsView.jsx` resta **una sola riga**: la label `card_counter_manual: 'Carta/POS al banco'` (senza, `getMethodLabel` cadeva sul provider tecnico grezzo). **Test alignment:** riallineati i 2 test obsoleti di `kitchen-service-pressure.spec.js` che `9a10612` aveva lasciato indietro — il bottone unico `PAGATO` non esiste più (ora `CONTANTI ✓` / `CARTA/POS ✓`, con RPC mockata e asserzione che i due bottoni mappano su `['cash','card_counter_manual']`, più la conferma esplicita richiesta sulla carta), e le asserzioni ALERT sono scopate per sezione perché gli allergeni sono raggruppati **per ordine e non più per tavolo** (stesso nickname in 2 sezioni → prima era strict-mode violation); aggiunto `.ksd-row-table` count 0 e rimosso il campo morto `table` dalle 9 fixture dello spec; il flusso di annullamento è stato estratto in un test `2b` a sé. **Nuovo spec `tests/e2e/kitchen-payments-refund-contract.spec.js` (4 test)**: fissa a test che la CTA RIMBORSA **non dipende dal provider** (attiva su Satispay e contanti, non solo SumUp), che resta esclusa dove non c'è nulla da rimborsare (charge fallito, ordine con refund già aperto) e che il metodo è mostrato in chiaro; gira su `/kitchen/staff-payments-demo` con `refundAction` simulata — **zero rimborsi reali, zero chiamate a SumUp/Supabase/api**, asserito nel test stesso. Guardrail verificato per contrasto: reintroducendo il gate `provider === 'sumup'`, 2 dei 4 test falliscono. **Suite:** `npm run build` PASS, `npm run test:unit` 272/272, unit migration 5/5, unit Realtime 2/2, E2E totali **68 passed / 2 failed / 3 skipped** (erano 61/4/3). I 2 failed residui sono **preesistenti e non regressioni**: sono i test F-SEC-2 del guard staff, che il config principale rompe perché forza `VITE_E2E_BYPASS_STAFF_AUTH=true`; sotto il loro config dedicato `playwright.fsec2.config.js` fanno **3 passed / 3 skipped**. Migration draft, screenshot, promo e artefatti **esclusi dal commit, non cancellati né rinominati**; nessun `db push`/`db repair`, nessuna scrittura DB/env, nessun push/deploy.

- **Kitchen — STAFF UX CONSOLIDATION FASE 2: rimozione fisica legacy TEAM.** Rimossi `src/pages/KitchenStaffDashboard.jsx`, `src/pages/CounterOrdersView.jsx`, `src/pages/KitchenOrdersView.jsx`: erano già irraggiungibili (nessuna route in `App.jsx` li montava più, `/kitchen/staff` → `KitchenStaffRedirect` → `/kitchen/solo`), unico consumer reciproco (`KitchenStaffDashboard.jsx` importava gli altri 2), zero import esterni residui verificati per grep prima e dopo. `/kitchen/solo` resta l'unica UI staff operativa; `/kitchen/staff` resta compat redirect (invariato). Potate da `KitchenStaffDashboard.css` (654→367 righe) solo le 34 classi usate esclusivamente nei 3 file rimossi (badge header, tab bancone/cucina/menu/alert, production bar, ready-alert, cancel panel, row-actions/note-block, `.ksd-btn-action`, incluse le loro varianti nei due media query) — verificato per ognuna zero occorrenze altrove nel repo prima di rimuoverla. Lasciate intatte: `.ksd-badge-active`/`.ksd-history-toggle*` (dead code preesistente, non correlato a questa rimozione, fuori scope) e tutte le classi ancora usate da `KitchenStaffDashboardDemo.jsx` (`/kitchen/staff-payments-demo`, isolata, invariata), `KitchenTrainingDemo.jsx` e `KitchenSoloService.jsx`/`StoricoView.jsx`/`AlertView.jsx`/`MenuView.jsx`. Nessun commento/test aggiornato: i riferimenti residui a `KitchenStaffDashboard`/`CounterOrdersView`/`KitchenOrdersView` in commenti (`KitchenStatusBadge.jsx`, `KitchenPayments.jsx`, `useKitchenOrders.js`, test E2E) sono già formulati al passato/come confronto storico ("legacy", "Solo Service non ha... di CounterOrdersView"), nessuno cita TEAM come UI operativa corrente. **Verifica:** `grep -rn "KitchenStaffDashboard\.jsx\|CounterOrdersView\|KitchenOrdersView"` in `src/` → zero import attivi residui. `npm run build` PASS. `npm run test:unit` 272/272 PASS. E2E mirati: `kitchen-solo-service.spec.js` 17/17, `customer-kitchen-flow.spec.js` 26/26 (1 skip preesistente non correlato), `kitchen-service-pressure.spec.js` 10/10, `kitchen-solo-demo.spec.js` 6/6, `kitchen-training-demo.spec.js` 4/4 — totale 64 passed/1 skipped. `f-sec-2-kitchen-guard.spec.js` sotto `playwright.fsec2.config.js` (guard reale, no bypass) verificato separatamente. Nessun DB/migration/auth/env/deploy toccato. **Committato in `9f8b825`** (vedi voce STAFF UX CONSOLIDATION — commit finale qui sotto), non pushato.
- **Kitchen — STAFF UX CONSOLIDATION — commit finale (2026-09-09, `integration/fantawalrus-kitchen-v1`, commit locale `9f8b825` sopra `ba6dd7a`, NON pushato).** Chiude in un solo commit la consolidazione staff avviata nelle sessioni precedenti (FASE 1 routing + FASE 2 rimozione fisica sopra), tutte insieme non ancora committate fino ad ora:
  - **`/kitchen/solo` è l'unica UI staff**: `KitchenStaffDashboard.jsx`/`CounterOrdersView.jsx`/`KitchenOrdersView.jsx` rimossi dal repo (335+200+175 righe, più 288 di CSS potato), zero import residui.
  - **`/kitchen/staff` resta compat redirect**: `src/App.jsx` monta `KitchenStaffRedirect` (nuovo file, `window.location.replace('/kitchen/solo')`) al posto del vecchio `KitchenStaffDashboard`.
  - **Post-login → Solo**: `KitchenLogin.jsx` naviga a `/kitchen/solo` (non più `/kitchen/staff`) dopo login riuscito.
  - **Legacy TEAM rimosso**: nessuna route/UI residua monta la vecchia dashboard a tab bancone/cucina/menu/alert.
  - **Solo supporta cash + card_counter_manual**: `KitchenSoloService.jsx` — pagamento banco split in due azioni esplicite (contratto RPC invariato `kitchen_payment_record_counter`, `p_method: 'cash' | 'card_counter_manual'`), sia nel flusso focus (`runAction('pay', ..., 'card_counter_manual')`) sia nel quick-pay di coda (`quickPay(..., 'card_counter_manual')`).
  - **Logout disponibile in ALTRO**: `handleLogout` (stesso flow `supabaseAuth.signOut` già usato dalla vecchia dashboard) esposto con `data-testid="logout-btn"` nella vista/tab ALTRO di Solo Service.
  - **`/kitchen/entry` redirect race corretto**: `CustomerKitchenEntry.jsx` passa da `history.replaceState` + `PopStateEvent` sintetico (soggetto a race se l'effect figlio gira prima che `App.jsx` registri il listener `popstate`, causando pagina bianca) a `window.location.replace('/kitchen')` — stesso fix già applicato a `KitchenStaffRedirect.jsx`.
  - **Test aggiornati nello stesso commit**: `tests/e2e/customer-kitchen-flow.spec.js`, `tests/e2e/f-sec-2-kitchen-guard.spec.js`, `tests/e2e/kitchen-service-pressure.spec.js` riallineati al nuovo flusso redirect/pagamento.
  - **Verificato in questa sessione (2026-09-09), su HEAD `9f8b825`:** `npm run build` PASS; `npm run test:unit` 272/272 PASS. **Baseline F-SEC-2 nota, invariata**: sotto `playwright.config.js` (che forza `VITE_E2E_BYPASS_STAFF_AUTH=true` sull'intera `testDir`) i 2 test del guard staff (T1, P1) falliscono per costruzione — non eseguono mai il guard reale; sotto il config dedicato `playwright.fsec2.config.js` (nessun bypass) il guard risulta verde. Nessuna regressione: comportamento datato, già documentato nel `NEXT STEP` come decisione di config aperta (escludere lo spec dal config principale o spostarlo), non riverificato E2E in questa sessione oltre a build/unit — solo allineamento CHECKPOINT, nessuna modifica codice.
  - File toccati (`git show --stat 9f8b825`): `CHECKPOINT.md`, `src/App.jsx`, `src/pages/CounterOrdersView.jsx` (rimosso), `src/pages/CustomerKitchenEntry.jsx`, `src/pages/KitchenLogin.jsx`, `src/pages/KitchenOrdersView.jsx` (rimosso), `src/pages/KitchenSoloService.css`, `src/pages/KitchenSoloService.jsx`, `src/pages/KitchenStaffDashboard.css` (rimosso), `src/pages/KitchenStaffDashboard.jsx` (rimosso), `src/pages/KitchenStaffRedirect.jsx` (nuovo), 3 spec E2E.
- **Kitchen — Promo Pass Redemption V2 (Opzione A) applicata al remoto e verificata live (2026-09-10, `pcrqfdzipotprqtuemso`, Gate 2 approvato da Eros).** Migration `20260910120000_kitchen_promo_pass_redeem_v1.sql` applicata via `supabase db push --linked` — unica migration nel batch, `migration list` post-push 32/32 local=remote, zero drift. Schema: `kitchen_orders.discount_amount` (numeric, default 0, check ≥0) + `promo_code` (text) live; RPC `kitchen_promo_pass_redeem_for_order(p_code, p_order_id)` SECURITY DEFINER live. **Smoke test reale controllato (dati creati e ripuliti nella stessa sessione, 0 residui):** redeem su ordine test con Peso Massimo (`item-009`, 13,90€) → sconto 10% corretto (1,39€), `total` 12,51€, promo pass → `status='redeemed'`/`redeemed_at`/`redeemed_by` valorizzati; **idempotenza PASS** (retry stesso codice sullo stesso ordine → ordine invariato, nessun doppio sconto); **doppio uso bloccato** (stesso codice su un secondo ordine → `promo_already_redeemed`); **Payment Hub verificato**: `kitchen_payment_record_counter` sull'ordine scontato registra `amount=12,51€` (legge `kitchen_orders.total` già scontato, zero modifiche alla RPC Payment Hub). Client-side (non ancora committato, vedi WORKTREE/NEXT STEP): `useKitchenOrders.js` (`redeemPromo`), `KitchenSoloService.jsx/.css` (bottone CODICE PROMO + badge), `KitchenPromo.jsx` (copy aggiornata), 4 nuovi test E2E in `kitchen-solo-service.spec.js`. Report: `ai-ops/reports/kitchen-promo-redeem-v2-implementation.md`.
- **Kitchen — Promo Pass Redemption V2, Opzione 2 (customer self-redeem) — live sul remoto e verificata (2026-09-10, `pcrqfdzipotprqtuemso`, Gate 1 + Gate 2 approvati da Eros).** Decisione di prodotto: il cliente inserisce il codice nel drawer carrello del proprio ordine (non più lo staff); errore promo sempre visibile al cliente; SOLO Service mantiene solo un badge passivo. Migration `20260910130000_kitchen_promo_pass_redeem_customer_v1.sql` applicata via `supabase db push --linked` — unica migration pending, `migration list` post-push **33/33 local=remote, zero drift**. Unica riga di logica cambiata nella RPC `kitchen_promo_pass_redeem_for_order` (già live da Opzione A): gate esteso da `is_staff_for_venue(...)` a `is_staff_for_venue(...) OR v_order.customer_id = auth.uid()` — resto della funzione (lock, retry idempotente, allowlist Pesi Massimi, sconto -10%, guardia anti-doppio-uso) invariato. **Smoke reale controllato con 2 identità cliente anonime reali e distinte** (create via `signInAnonymously()` come farebbe un cliente vero, non righe fabbricate): **customer self-redeem live** — cliente A redime il proprio codice sul proprio ordine dal drawer reale dell'app (**owner redeem PASS**, ordine A04, −€1,39, totale €12,51); cliente B tenta il proprio codice valido sull'ordine di A → `not_authorized_for_order` (**foreign customer blocked**); staff reale (via `kitchen_staff_members`, nessun account creato) continua a poter redimere qualunque ordine del venue (**staff access PASS**, ordine A05, −€1,45, totale €13,05); sconto verificato -10% su un solo Peso Massimo su entrambi gli ordini, arrotondato a 2 decimali (**-10% Peso Massimo PASS**); retry stesso codice/stesso ordine → invariato, nessun doppio sconto (**idempotenza PASS**); stesso codice già redento su un ordine diverso → `promo_already_redeemed` (**double-use blocked**); `kitchen_payment_record_counter` sull'ordine scontato registra `amount=€12,51`, identico al totale già scontato, zero modifiche a quella RPC (**Payment Hub total PASS**). **Cleanup**: tutte le righe di dominio create dal test (2 ordini, 2 righe item, 1 pagamento, 1 action log, 2 promo pass) cancellate e verificate a 0 residui (**cleanup PASS**); i 2 account anonimi reali sono stati lasciati intenzionalmente (stesso pattern del precedente smoke V2, sono indistinguibili da un cliente reale, nessun dato personale). Client-side: input codice in `CustomerKitchenMenu.jsx` (drawer carrello, redeem dopo `addOrder()` e prima della conferma, banner successo/errore sempre visibile su ORDINE RICEVUTO), rimosso bottone/handler promo da `KitchenSoloService.jsx`/`.css` (resta solo il badge passivo, nessuna dipendenza da `redeemPromo`), test E2E aggiornati (`kitchen-solo-service.spec.js`: 4 test staff-driven obsoleti sostituiti con 2 mirati; `customer-kitchen-flow.spec.js`: +2 test promo valida/non valida). Build PASS, `test:unit` 272/272, E2E mirati 49/49 + 1 skip preesistente non correlato, zero regressioni. Report: `ai-ops/reports/kitchen-promo-customer-redeem-implementation.md`, `ai-ops/reports/kitchen-promo-customer-redeem-gate2-remote-apply.md`.
- **Kitchen — BEER SPRINT V1, Fase A+B (2026-09-14, `integration/fantawalrus-kitchen-v1`, NON committato, NON pushato).** A0 audit read-only (`ai-ops/reports/beer-sprint-v1-a0-audit.md`) ha confermato: catalogo `birre` con solo 2 item legacy fake (`item-005/006`), categoria esclusa dalla nav cliente, nessun modello `availability`/`choiceLabel`, nessuna sezione dedicata, zero analytics nel repo. Decisioni Eros: `birre` categoria primaria prima di `bevande`; analytics rimandata a Fase F; BirreSection dedicata (fallback generico non bastava per la choice architecture); Krombacher modellata `evening_only` ma non ordinabile finché formato/prezzo non confermati. **Fase A (dati, `src/data/kitchenMockData.js`):** aggiunti 7 item reali `birre-v1` (`item-051`→`057`) — 6 bottiglie Keiler×5 + Lupulus, tutte €6 (Lupulus 33 cl, le altre 50 cl), campi nuovi `format`/`choiceLabel`/`tasteSignals`/`availability` (`all_day`), allergene `glutine`; Krombacher Pils (`item-057`) `availability: 'evening_only'`, `price`/`format: null` → resta non ordinabile (stesso pattern "PREZZO IN ARRIVO" già in uso). I 2 item legacy (`item-005/006`) restano nei dati/ordini demo storici, invariati. **Fase B (UI):** `birre` aggiunta a `MENU_CATEGORIES` in `CustomerKitchenMenu.jsx` (posizione prima di `bevande`, icona `CATEGORY_SVGS.birre` già esistente), `item-005/006` aggiunti a `CUSTOMER_HIDDEN_ITEM_IDS` (non più esposti ora che `birre` è navigabile), subtitle categoria aggiunto. Nuovo componente dedicato `src/components/kitchen/BirreSection.jsx` + `.css` (pattern closed-only, nessun frame Figma per questa categoria, stesso linguaggio CTA/colore del resto del menu): card con nome, `choiceLabel`, max 2 taste signal, formato, prezzo, allergeni, CTA `LO VOGLIO`/`ESAURITO`/`PREZZO IN ARRIVO`/`SOLO LA SERA` (evening gating lato client, soglia 18:00 locale — assunzione dichiarata, da confermare con Eros, non bloccante perché Krombacher resta comunque non ordinabile per `price: null`). **Verificato:** `npm run build` PASS, `npm run test:unit` 272/272 PASS. E2E: confermato **zero regressioni** confrontando con un baseline isolato (stash temporaneo, mai un `git checkout` distruttivo) — `customer-kitchen-flow.spec.js` 24 passed/7 failed identici pre/post (i 7 failed sono pre-esistenti, env `PGRST202`/schema cache Supabase, non correlati), `kitchen-solo-service.spec.js` 21 passed/1 failed identico pre/post (già noto). **QA reale in browser** (Chromium 390×844, override di processo senza toccare `.env.local`): categoria BIRRE visibile in CATEGORIE ("BIRRE 7 VOCI", prima di BEVANDE), tile → sezione dedicata con le 7 card corrette (nomi/choiceLabel/tasteSignals/formato/prezzo tutti corretti), Krombacher disabilitata "SOLO LA SERA" (test eseguito di giorno), add-to-cart reale su Keiler Helles → bottom bar "1 ROBA NEL SACCO €6,00". Nessun file in area protetta toccato oltre a `CustomerKitchenMenu.jsx` (STABLE, non DO NOT TOUCH — modifica scoped, richiesta esplicitamente dalla missione). Report: `ai-ops/reports/beer-sprint-v1-a0-audit.md`; missione: `ai-ops/current/BEER_SPRINT_V1.md`.

- **Kitchen — BEER SPRINT V1, Fase E, parte 1 — Krombacher confermata + audit combo (2026-09-14, `integration/fantawalrus-kitchen-v1`, NON committato, NON pushato).** Dati confermati da Eros: Krombacher Pils alla spina **€6**, servizio serale dalle **18:00** locali, `availability: 'evening_only'`, formato in cl **non confermato** (non inventato). `src/data/kitchenMockData.js` — `item-057.price: null → 6.0` (gate 18:00 già esistente da Fase B). `src/components/kitchen/BirreSection.jsx` — commento aggiornato. **Audit combo reali:** l'unico meccanismo combo attivo nel menu cliente è **FALLO PESANTE** (`kitchenPesiMassimiCombos`, `PesiMassimiSection.jsx`); `item-007`/`item-008` (FALLO COMBO) restano SUPERATI, non navigabili, non toccati.
- **Kitchen — BEER SPRINT V1, Fase E, parte 2 — birra scelta in FALLO PESANTE (2026-09-14, `integration/fantawalrus-kitchen-v1`, NON committato, NON pushato).** Decisione prodotto di Eros ricevuta dopo la parte 1: il prezzo di FALLO PESANTE **resta invariato**, il cliente **sceglie 1 birra inclusa** tra le 6 bottiglie + Krombacher (dalle 18:00). Implementato: **nuovo file condiviso `src/lib/kitchenServiceRules.js`** (`EVENING_SERVICE_START_HOUR = 18`, `isEveningServiceActive()`) — unica fonte del gate serale, importato sia da `BirreSection.jsx` (refactor, stesso comportamento) sia dal nuovo selettore, per non duplicare la regola. **`src/components/kitchen/PesiMassimiSection.jsx`**: nuovo prop `beerOptions` (default `[]`, retrocompatibile — `/kitchen/promo` che passa `hideCombo` non è impattato), selettore a pillole dentro il blocco FALLO PESANTE (6 birre sempre selezionabili, Krombacher disabilitata con badge "SOLO LA SERA" fuori orario), CTA FALLO PESANTE **disabilitata finché non è scelta una birra**. **Prezzo del combo non toccato**: `combo.price` resta l'unico prezzo, nessuna logica nuova — la birra scelta va solo nel `name` della riga carrello ("Pulled Pork — Fallo Pesante · Keiler Helles"), mentre l'`itemId` inviato all'ordine resta il vero id del combo invariato (`item-040`/`041`/`042`) per non rompere l'allowlist del redeem promo (`kitchen_promo_pass_redeem_for_order`, che verifica `oi.item_id = ANY([...'item-040','item-041','item-042'])`) né alcuna altra logica server. Per distinguere comunque le quantità quando lo stesso combo è scelto due volte con birre diverse, la riga carrello usa un `id` composito solo lato UI (`combo.id::beerId`) con un nuovo campo `baseId` (= vero id del combo) che `CustomerKitchenMenu.jsx` (`addItem`, `handleSubmit`) usa per il payload reale — cambio isolato e retrocompatibile (`baseId` di default = `id` per ogni altro prodotto, comportamento invariato). `falloPesanteBeerOptions` (catalogo derivato, filtro tag `birre-v1`, mai le birre legacy nascoste) passato solo alla card BBQ del menu cliente reale. CSS aggiunto in `PesiMassimiSection.css` (`.pm-upsell-beer-*`, `.pm-beer-pill*`, token colore riusati `--pm-gold`/`--pm-sand`/`--pm-cream`/`--pm-bg`, nessun nuovo linguaggio visivo). `kitchenBeerPairing` (auto-selling Fase D, drawer carrello) **lasciato invariato**, fuori scope di questa decisione. **Test:** `npm run build` PASS, `npm run test:unit` 272/272 PASS. Due nuovi test E2E in `tests/e2e/customer-kitchen-flow.spec.js`: **20** (Krombacher in BIRRE, `page.clock` pre/post 18:00, CTA/prezzo/add-to-cart) e **21** (FALLO PESANTE: CTA disabilitata senza birra scelta, prezzo combo invariato €19 prima e dopo la scelta, Krombacher bloccata prima delle 18:00 con badge "SOLO LA SERA" e selezionabile dopo, riga carrello con nome birra e prezzo combo corretto in entrambi i casi) — entrambi PASS. **Regressione misurata** (`--workers=1`, per escludere flakiness da esecuzione parallela nota — con più worker un test panini isolato è emerso come flaky-solo-in-parallelo, confermato passante da solo e nella run single-worker): `customer-kitchen-flow.spec.js` **26 passed/7 failed/1 skipped** (baseline Fase A+B 24/7 + 2 nuovi test = 26, stessi 7 falliti pre-esistenti `PGRST202`/schema cache, zero regressioni). `kitchen-solo-service.spec.js` **27/1** e `kitchen-service-pressure.spec.js` (insieme, single-worker) — i **3 falliti** (`2.`/`2b.`/`2c.`, incasso banco) sono stati **riverificati identici sulla baseline pre-Fase-E** (stash temporaneo, git stash apply/drop, mai un checkout distruttivo) — stessi 3 test, stesso errore, **zero regressioni**, pre-esistenti e non correlati (env Supabase). Nessun file in area protetta toccato oltre a quelli elencati. **Nessun OPEN INPUT residuo su questa decisione**: la richiesta di Eros è stata implementata esattamente come specificata (prezzo invariato, scelta birra reale, Krombacher su `evening_only`).
- **Kitchen — BEER SPRINT V1, Fase C+D (2026-09-14, `integration/fantawalrus-kitchen-v1`, NON committato, NON pushato).** **Fase C (asset, audit-only, zero file creati/modificati):** cercati asset reali per le 6 bottiglie Keiler + Lupulus + Krombacher in `public/assets/kitchen/` — trovati solo 2 file storici (`photo-birra-del-tricheco.webp`, `photo-birra-scura-problematica.webp`), che sono le foto dei 2 item legacy fake "Birra del Tricheco"/"Birra Scura Problemática" (`item-005/006`, nomi/prodotti inventati pre-missione, non birre reali) — **nessun asset reale riusabile per i 7 item `birre-v1`**. Confermato che i 7 item hanno già `image: null` (Fase A) e che `BirreSection.jsx` ha già il sistema di fallback coerente: stesso box 96px, stesso trattamento `object-fit: contain`, stesso placeholder icona 🍺 su tutte le card indipendentemente dalla birra — la "famiglia visuale" richiesta è già garantita a livello di sistema, pronta ad accogliere foto reali in futuro senza toccare il componente. **STOP applicato solo sulla parte asset mancante** (§7-C missione): nessuna immagine generata o placeholder-prodotto creato senza approvazione. Nessun file toccato in questa fase. **Fase D (auto-selling V1):** aggiunta matrice configurabile `kitchenBeerPairing` (`src/data/kitchenMockData.js`, dopo `kitchenCategoryPromos`) — `byCategory`: panini→Helles (item-051), cicchetti→Land-Pils (item-052), bbq/Pesi Massimi→Lupulus (item-056), tagliere→Kellerbier (item-053), insalatone/tartare→Weisse (item-054); `byItem` vuoto ma pronto per override puntuali futuri (priorità item > categoria). Nessun motore AI: lookup puro, un solo suggerimento per contesto. Integrazione in `CustomerKitchenMenu.jsx`: nuova funzione `findRecommendedBeer(orderItems, menuItems)` (mai birra se il sacco ne contiene già una, mai birra a prezzo non confermato/esaurita — Krombacher quindi mai suggerita) + banner "CI STA BENE UNA BIRRA?" nel drawer carrello, tra la lista articoli e "DOVE LO MANGI?" (unico punto di iniezione, nessuna sezione categoria toccata — **`PesiMassimiSection.jsx`/FALLO PESANTE non modificato**, integrazione avviene solo a valle nel drawer). CTA esplicita (click per aggiungere, mai add automatico), sparisce da sola se la birra suggerita è già nel sacco. CSS aggiunto in `CustomerKitchenMenu.css` (`.kitch-pairing-*`, token colore/font riusati dal resto del drawer). **Verificato:** `npm run build` PASS, `npm run test:unit` 272/272 PASS, `customer-kitchen-flow.spec.js` **24 passed/7 failed — identico al baseline Fase A+B** (i 7 falliti restano gli stessi pre-esistenti `PGRST202`/schema cache Supabase, non correlati, zero regressioni). QA reale mirata (Chromium, override di processo senza toccare `.env.local`): aggiunto un Peso Massimo al sacco → banner mostra "LUPULUS" (match esatto matrice bbq→Lupulus) → click aggiunge Lupulus alla lista drawer → banner sparisce (birra già nel sacco), test poi rimosso (non committato, solo QA). File toccati: `src/data/kitchenMockData.js`, `src/pages/CustomerKitchenMenu.jsx`, `src/pages/CustomerKitchenMenu.css`. Analytics (`beer_pairing_impression`/`beer_pairing_accept`) volutamente non implementate: rimandate a Fase F per decisione Eros (invariato).

- **PRE-PUSH BLOCKER 3 — RESOLVED (riconciliato 2026-09-09, pre-push gate read-only su HEAD `1987d5e`).** Il bug "dopo il back da ORDINE RICEVUTO il cliente resta bloccato" (`CustomerKitchenMenu.jsx`, ramo `submittedRef` di `handlePopState`) era già stato risolto il 2026-09-04 dal commit `c1e8b25` ("fix(kitchen-customer): unblock new orders after status back-nav, restore allergen visibility"), che aggiunge `setSubmitting(false)` + `setOrderItems([])` + `setSubmittedOrderId(null)` + `setSubmittedOrderCode(null)` a quel ramo (`:234-237`). Il CHECKPOINT era rimasto disallineato, ancora con il blocker in OPEN ISSUES come "fix proposto, non applicato". Verificato in questa sessione: `c1e8b25` è ancestor di HEAD `1987d5e` (`git merge-base --is-ancestor` conferma), `git blame` sulle righe 232-241 di `CustomerKitchenMenu.jsx` mostra il codice attuale invariato da quel commit, `git diff HEAD` sul file è vuoto. Nessun codice toccato in questa sessione, solo riconciliazione doc.

- **Kitchen — Customer Payment & Order Lifecycle Sprint — CHIUSO e verificato (2026-09-18, working tree locale, NON committato, NON pushato).** Audit read-only (`ai-ops/reports/kitchen-payment-customer-lifecycle-audit-2026-09-18.md`) → implementazione (`ai-ops/reports/kitchen-payment-customer-lifecycle-implementation-20260918.md`) → fix pass AC4 → final review read-only (`ai-ops/reports/kitchen-payment-customer-lifecycle-ac4-final-review-20260918.md`, **VERDICT: PASS**). File toccati: `src/pages/CustomerOrderStatus.jsx`, `src/pages/CustomerKitchenMenu.jsx`, `tests/e2e/customer-kitchen-flow.spec.js` (+2 test, 30-31). Nessuna migration/RLS/RPC toccata (confermato non necessario da audit iniziale).
  - **AC1 — bivio pagamento: COMPLETATO E VERIFICATO.** Dopo "Conferma ordine", `CustomerOrderStatus.jsx` mostra un bivio esplicito a scelta unica **PAGA IN CASSA / PAGA ONLINE** (`data-testid="ost-payment-fork"`) prima di qualunque pannello di pagamento — sostituisce la coesistenza precedente dei due pannelli. Scelta persistita per ordine in `localStorage` (`walbox_kitchen_payment_choice_<orderId>`), sopravvive al refresh, "PAGA ONLINE" richiama lo stesso `handlePaySumup()` esistente (nessuna logica di checkout duplicata).
  - **AC3 — SumUp abandon/recovery: COMPLETATO E VERIFICATO.** La reconciliation (`api/kitchen-sumup-reconcile`) è ora la funzione riusabile `runReconciliation()`, richiamabile sia dall'auto-run al mount (`?sumup=return`) sia dal nuovo bottone **VERIFICA DI NUOVO** sugli stati `pending`/`unknown` — prima il retry mutava solo un ref morto (`reconcileStartedRef`) e non rieseguiva mai la verifica. Fallback cassa (`showCashFallback`) resta sempre raggiungibile su `error`/`pending`/`unknown`, mai durante checkout attivo. Annullamento esplicito lato cliente resta fuori scope (non richiesto dai 5 AC).
  - **AC4 — staff cancel: COMPLETATO E VERIFICATO (fix pass dedicato).** Root cause: `kitchen_order_cancel` (migration `20260914090000_kitchen_order_cancel_guard_v1.sql`) aggiorna solo `status`/`cancel_reason`/`cancelled_at`, **mai** `payment_status` — un ordine annullato prima del pagamento resta quindi con `payment_status='pending_counter_payment'` per costruzione, e la prima passata di AC1 (basata su un OR fra `status` e `paymentStatus`) lo trattava ancora come "in attesa di pagamento", mostrando bivio/pannelli su un ordine già annullato. Fix in `CustomerOrderStatus.jsx`: (1) guard `isClosedStatus` (`cancelled`/`delivered`) valutato **prima** di `isPendingPayment`, che diventa sempre `false` su stato chiuso indipendentemente da `payment_status`; (2) nuovo `useEffect` che, se l'ordine correntemente in vista diventa `cancelled`, esce subito dalla vista attiva — passa a un altro ordine proprio ancora attivo se esiste (`location.replace` verso quell'`orderId`), altrimenti torna a `/kitchen` — stesso pattern `location.replace` già in uso in `CustomerKitchenEntry.jsx`/`KitchenStaffRedirect.jsx` (2026-09-09) per evitare la stessa race di mount col listener `popstate` di `App.jsx`. `delivered` resta invariato, non toccato da questo redirect (fuori scope dichiarato per questo fix). Coperto da 2 nuovi test E2E (30: unico ordine cancelled → torna al menu; 31: cancelled + altro ordine attivo → passa automaticamente a quello).
  - **AC5 — delivered: COMPLETATO E VERIFICATO (invariato dalla prima implementazione).** Banner "SEGUI IL TUO ORDINE" in `CustomerKitchenMenu.jsx` deriva ora dallo stato reale (`orders` da `useKitchenOrders`, reattivo poll 10s + realtime + storage event) filtrato per proprietà, escludendo esplicitamente `delivered`/`cancelled` — sparisce nello stesso render in cui lo stato arriva, nessuna permanenza artificiale, nessun `setTimeout`.
  - **Final reviewer: PASS.** Review read-only dedicata (non lo stesso pass di implementazione) ha verificato: root cause a livello DB (migration letta riga per riga), correttezza del guard e del redirect, precedente reale per `location.replace` (verificato nei 2 file citati, non un'invenzione nel commento), che i 2 nuovi test coprono davvero entrambi gli scenari runtime (eseguiti live in review, non solo letti), e assenza di regressioni AC1/AC3/staff.
  - **Test/evidence finali (eseguiti sia in implementazione sia in final review):** `npx playwright test tests/e2e/customer-kitchen-flow.spec.js` → **43 passed / 1 skipped** (skip pre-esistente non correlato; include i nuovi test 30/31 AC4). `npx playwright test tests/e2e/kitchen-solo-service.spec.js` → **22/22 PASS** (staff, non toccato). `npx playwright test tests/e2e/kitchen-service-pressure.spec.js` → **8 passed / 1 failed** — il fallito è il test **2b** (annulla ordine), **failure pre-esistente e non correlato**: `order_not_found`, fixture non presente nel Supabase reale, già a registro in OPEN ISSUES/DONE prima di questo sprint, riprodotto identico. `npm run test:unit` → **272/272 PASS**. `npm run build` → **PASS** (solo warning pre-esistente su chunk size). `npx eslint` sui 2 file toccati: 0 nuovi errori (stessa famiglia di violazioni pre-esistenti già presenti nel repo).
  - **AC7 — multi-ordine stacked: IMPLEMENTATO (2026-09-18).** Sostituito lo switcher orizzontale "I MIEI ORDINI" (card compatta, un solo ordine "corrente" alla volta) con una vista impilata: `CustomerOrderStatus.jsx` è ora un container sottile (topbar, empty state, dev tools) che filtra `myOrders` escludendo `cancelled`/`delivered` e monta un `CustomerOrderPanel` per ciascun ordine attivo rimasto, **senza cap** (era `.slice(0,5)` sullo switcher, rimosso). Nuovo file `src/components/kitchen/CustomerOrderPanel.jsx`: contiene TUTTO il rendering e lo stato precedentemente nel container per un singolo ordine (hero, bivio pagamento AC1, pannello SumUp/cassa, info grid, status banner con **codice ordine grande e dominante** `.ost-order-code-big`, timeline, items, note, ponte jukebox, CTA inline) — stato SumUp, ref di reconciliation, scelta pagamento (`localStorage` per `orderId`) e CTA sono locali al componente, mai condivisi fra ordini montati in parallelo. La fixed bottom bar globale è rimossa: ogni ordine ha la propria CTA inline (`.ost-inline-cta`, non `position:fixed`) dentro il proprio blocco. Reconciliation al ritorno `?sumup=return`: il container calcola una sola volta `sumupReturnTargetId` (stesso guard di proprietà di `resolveUrlOrderId`, invariato) e lo passa come prop `isReturnTarget` **solo** al pannello con quell'id — nessun fallback sul più recente. L'effect "esci dalla vista se cancelled" del vecchio container è stato rimosso: non serve più, un ordine cancelled semplicemente non genera più un blocco al render successivo (nessun "ordine corrente" da cui allontanarsi). `KitchenOrderCard.jsx` non toccato (import rimosso da `CustomerOrderStatus.jsx`, il file resta ma non è più usato in questo flusso). Nessun file Payment Hub/DB/RLS/KitchenPrepBoard/routing toccato. **Test:** aggiornati i test 28 (switcher → `.ost-order-block`/`.ost-order-code-big`) e 30/31 (AC4: niente più redirect URL a un "ordine successivo", ora è semplicemente assente dalla lista attiva) per la nuova architettura senza "ordine corrente"; aggiunti 5 nuovi test AC7 (32-36): nessun cap con 6 ordini attivi, stati/codici indipendenti su 2 ordini, cancelled+delivered esclusi anche con altri ordini presenti, bivio/CTA pagamento indipendenti su 2 ordini pending (click su uno non tocca l'altro), reconciliation SumUp al ritorno solo per l'ordine target (mock storage session + route `/api/kitchen-sumup-reconcile`, verificato che l'altro ordine non genera mai quella chiamata). `customer-kitchen-flow.spec.js`: **48 passed / 1 skipped** (skip pre-esistente non correlato). `npm run test:unit` 272/272 PASS. `npm run build` PASS. `npx eslint` sui file toccati: 1 solo errore, **pre-esistente e invariato** (`no-empty` sul catch di `readOwnedOrderIds`, presente identico su HEAD) — il refactor riduce anzi gli errori lint rispetto a HEAD (spariti `tick` inutilizzato e la mutazione di variabile a livello modulo, entrambi legati allo stato ora isolato nel pannello). `kitchen-solo-service.spec.js` 22/22 PASS (staff, non toccato). `kitchen-service-pressure.spec.js` 8 passed/1 failed, **failure pre-esistente e non correlato** (test 2b, `order_not_found`, fixture non presente nel Supabase reale, già a registro). Non riverificato: `playwright.fsec2.config.js` (gap ambientale noto, non bloccante, invariato).
  - **Scope non toccato:** `KitchenPrepBoard*`/`/kitchen/prep*` (WIP separato, Production Board) e `src/lib/kitchenAllergens.js`/`src/App.jsx` (stesso WIP separato) non fanno parte di questo sprint — modifiche preesistenti nel working tree, non toccate né riviste qui. Nessun commit/push/deploy eseguito.

## STABLE — non toccare senza approvazione
- **Checkpoint locale salvato (2026-07-05): commit `2f06353` "chore: save ai-ops factory and walbox visual updates"** — contiene ai-ops AI Factory alignment, SECURITY_POLICY.md, reports/knowledge placeholders, modifiche visual/app Walbox, FABLE_WALBOX_CREATIVE_DIRECTION_PACK. Non pushato (vedi OPEN ISSUES).
- src/hooks/useKitchenOrders.js, useKitchenMenu.js — dual-write Supabase+localStorage
- src/lib/supabaseClient.js / supabaseAuth.js
- src/pages/CustomerKitchenMenu.jsx, CustomerOrderStatus.jsx
- tests/e2e/customer-kitchen-flow.spec.js (17), kitchen-service-pressure.spec.js (4)
- playwright.config.js — porta 5174, reuseExistingServer:false

## DO NOT TOUCH
- useCustomerSession.js — condiviso jukebox+kitchen
- .env / .env.local / package.json — sempre protetti
- App.jsx — routing protetto
- Kitchen (hooks, e2e sopra) — stabile, fuori scope track Jukebox attuale. Il TEAM dashboard legacy
  (`KitchenStaffDashboard.jsx`) è stato rimosso il 2026-09-07 (vedi DONE): `/kitchen/solo` è l'unica
  UI staff operativa, non più "dashboard" da proteggere qui.
- **Migration ledger Kitchen riconciliato in locale (2026-09-07, `integration/fantawalrus-kitchen-v1`, NON committato, NON pushato).** Gate 1 read-only (`ai-ops/reports/kitchen-migration-ledger-reconciliation-20260907.md`) ha provato **sul contenuto** (md5, non sui nomi) che tutte le 6 migration locali che risultavano `pending` erano in realtà **già applicate sul remoto sotto una version rigenerata**: `20260830130000`→`20260830101227`, `20260830140000`→`20260830101340`, `20260830150000`→`20260830143353`, `20260901130000`→`20260903001022`, `20260906120000`→`20260906171426` (byte-identiche o SQL-identiche), e il draft `20260901120000` **spezzato su due** righe remote: §1 come `20260905072954`, §2+§3 come `20260906171426`. **Fatto che supera i report precedenti:** le RPC `kitchen_customer_create_order` e `kitchen_payment_record_counter` che `sprint3b-remote-db-alignment-audit.md` dava per mancanti **sono live dal 2026-09-06** — quel verdetto `BLOCKED` non è più valido. **Strategia A eseguita (Gate 2 approvato da Eros), solo file locali, zero scritture su Supabase:** rinominati i 5 `.sql` (+3 `.test.js`, cambia solo il path in `readFileSync`) sulla version realmente registrata; ricostruito il file mancante `20260905072954_kitchen_order_code_infra_table_id_nullable_v1.sql` (md5 delle sole righe SQL identico al ledger); corretti gli header che dichiaravano il falso `LOCAL DRAFT ONLY — not applied`; draft superato `20260901120000` **archiviato e non cancellato** in `ai-ops/archive/migrations/`; il suo test ritargetato sui **due file realmente applicati** e rinominato `20260906171426_kitchen_pilot_order_contract_rpc_v1.test.js`. **Nessun `db push`, `migration repair`, SQL mutativo, deploy, commit o push.** Verifiche: 31 file locali ↔ 31 righe remote, **0 pending, 0 remote-only**; md5 SQL di tutti e 6 i file combacia col ledger; `node --test supabase/migrations/*.test.js` **81/81 PASS**; `npm run test:unit` e `npm run build` PASS; schema remoto **invariato** (probe identica a prima). Regola anti-drift aggiunta in `ai-ops/SECURITY_POLICY.md` §5.1: l'apply passa solo da `supabase db push --linked`, mai da `apply_migration`/`db query -f`, che rigenerano la version ed erano la causa provata dei disallineamenti del 2026-08-28 e del 2026-09-07. **Non certifica nulla a runtime:** la certificazione runtime multi-device resta aperta e invariata.

## OPEN ISSUES
- Reality Sprint 02 ha confermato che queuePaused/venueSettings è ancora localStorage-only: funziona tra tab dello stesso browser, ma non tra dispositivi staff/clienti. Fix futuro richiede venue_settings su Supabase + hook realtime dedicato.
- FABLE_WALBOX_CREATIVE_DIRECTION_PACK (introdotto nel commit `2f06353`): tracked but not active — non è la priorità corrente, non cambia il NEXT STEP finché Eros non lo attiva esplicitamente.
- Fix "Sposta Su/Giù" in coda staff rimandato (menzionato in commit 553a86e, da verificare stato)
- Copertura E2E Jukebox parziale: oltre ai 17+4 Playwright Kitchen esistono `smoke.spec.js` e `spotify-search-ui.spec.js` (entry + ricerca/UI Jukebox, 25/25 PASS totali), ma manca ancora E2E reale su approvazione/coda/now-playing con Spotify Premium reale
- FIX APPLICATO (2026-07-05): `StaffDashboard.jsx` → `handleApprove` ora mostra warning esplicito quando il brano approvato non ha `spotify_uri` (non entrerà mai in coda Spotify automaticamente)
- S11 Supabase: localStorage writes ancora attivi — deferred per cross-tab sync
- CHECKPOINT era fermo al 3/7, disallineato dal commit CLAUDE.md f077276 del 4/7 — risolto con questo update (2026-07-05)
- **Migration ledger Kitchen — RISOLTO il 2026-09-07 (vedi DONE).** Il ledger remoto e i file locali sono allineati 31↔31, zero pending e zero remote-only. Restano validi i vincoli generali: l'apply passa solo da `supabase db push --linked` (`ai-ops/SECURITY_POLICY.md` §5.1). Testo storico del problema (2026-08-28 → 2026-09-07): `supabase migration list --project-ref pcrqfdzipotprqtuemso` mostra 8 voci `remote`-only senza file locale (`20260827234726`...`20260827235012`, verosimilmente lo schema Payment Hub applicato in passato fuori dal tracking migration) e le migration locali `20260828100000`→`20260828100700` risultano `remote=""` (non marcate applicate) pur essendo funzionalmente già live. Conseguenza concreta: `supabase db push`/`--dry-run` fallisce hard (`LegacyDbPushMissingLocalError`) e **non è utilizzabile così com'è** per nessun push futuro su questo progetto finché il ledger non viene riconciliato (`migration repair` + `db pull` mirato, task dedicato non ancora fatto). Il fix `20260828100800` è stato applicato aggirando il problema con `supabase db query -f` (solo quel file, nessun impatto sul ledger). Non bloccante per l'uso dell'app, ma bloccante per qualunque futuro `db push` su Kitchen finché non risolto.

- **[PRE-PUSH BLOCKER 1 — creativo] RISOLTO 2026-09-04.** Eros ha fornito la foto pulita; sostituita in `09ea248` con `cicchetto_scamorza_cipolle_v2.png` (1254×1254 PNG, come i 3 fratelli), riferimento aggiornato solo a `kitchenMockData.js:329`. Il file con watermark resta sul disco, ora orfano — e Vite lo copia comunque in `dist/`, quindi 2,4 MB di peso morto con watermark finiscono nel bundle di produzione pur non essendo raggiungibili dal cliente: rimuoverlo richiede approvazione. Testo originale del blocker:  `public/assets/kitchen/menu/cicchetti/cicchetto_scamorza_cipolle.png` ha la scritta **"Cicchetto 3"** stampata dentro l'immagine, visibile al cliente nella categoria CICCHETTI. Preesistente (committato in `e5e8378`), non introdotto dai 5 commit del 2026-09-04. Gli altri 3 cicchetti (`mortazza`, `lardo_noci`, `ciappi_veg`) sono stati verificati visivamente e sono puliti: il problema è isolato a questo singolo file. Come da direttiva non è stato inventato né generato alcun asset sostitutivo. Serve la foto pulita da Eros.
- **[PRE-PUSH BLOCKER 2 — test] RIDOTTO da 4 a 1 test (2026-09-04).** Misurato su worktree pulito a `09ea248`: `customer-kitchen-flow.spec.js` ora è **25 passed / 1 failed** (era 22/4). `3b`, `3e`, `3g` sono verdi, chiusi dai commit `5f28dc7` e `77d8912` già in HEAD. Resta rosso solo il **7** (`Bancone conferma pagamento → status received`), classificato **`ENV_INTEGRATION_REQUIRED`**: `useKitchenOrders.js:267` `confirmPayment()` chiama per prima cosa l'RPC `kitchen_payment_record_cash` e, se fallisce, esce con `applyLocalSyncStatus(...,'error')` + `return` senza applicare l'update locale — comportamento voluto, introdotto da P0-A per non produrre falsi successi su write fallite. In più l'avanzamento `pending_counter_payment → received` è implementato lato Postgres (`kitchen_payment_confirm`, migration `20260828100800`), non lato client. Senza un progetto Supabase reale che esponga l'RPC il test non può passare: non è una regressione né un difetto di codice. Testo originale del blocker:  `tests/e2e/customer-kitchen-flow.spec.js` 3b / 3e / 3g / 7 falliscono già a `ba41438` (baseline misurata su worktree pulito), quindi **non sono una regressione**. Causa diagnosticata: l'helper `openFullMenu()` (riga 29) si ferma alla schermata CATEGORIE mentre 3b/3e/3g asseriscono stato a livello di item (`ESAURITO`), che nella navigazione Figma nuova (`0009319` in poi) compare solo dopo essere entrati in una categoria; il 7 riguarda il lato banco (`PAGATO` dopo `IN ATTESA PAGAMENTO`). Il file è marcato STABLE: **non toccato**, serve task dedicato + approvazione di Eros per riallinearlo.
- **[operativo] `.env.local` fa crashare il mount in locale (2026-09-04):** contiene un `VITE_SUPABASE_URL` non valido (`Invalid supabaseUrl: Must be a valid HTTP or HTTPS URL`, `src/lib/supabaseClient.js:20`). Workaround usato per la QA locale, **senza modificare il file**: override di processo `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npm run dev` (fa cadere `isConfigured` a false e attiva il Proxy di fallback). Con quell'override `useKitchenOrders.js:183` emette 2 console error `Supabase non configurato` — artefatto del workaround, non un bug del percorso cliente.

- **[cross-domain, OPEN, NON modificare senza decisione dedicata] Bridge Jukebox `CustomerOrderStatus.jsx:601,612` finisce sempre sul tavolo hardcoded `'7'`:** `navigate('/request?table=' + (order.table || '') ... || '7')` — da quando Kitchen non ha più tavoli, `order.table` è sempre vuoto e il fallback `'7'` scatta sempre. Non toccato deliberatamente in Sprint 3B (RUN2, 2026-09-06): tocca il confine Kitchen/Jukebox, non solo Kitchen, e richiede una decisione di prodotto (a cosa deve puntare il bridge ora che il tavolo non esiste), non un fix tecnico isolato.

## REFERENCES
- Sub-agent routing: see CLAUDE.md section 2. Shuffle Night cascade: see feedback_shuffle_night_pipeline.md.
- Kitchen-era memories are archived; see MEMORY.md. Do not use them for the active Jukebox/Shuffle Night track.

## NEXT STEP
**Kitchen — Customer Payment & Order Lifecycle Sprint: AC1/AC3/AC4/AC5/AC7 tutti implementati e verificati (2026-09-18). Prossimo step: decisione di Eros su commit/push.** Vedi DONE per dettaglio/evidence di ciascun AC. AC7 (multi-ordine stacked) ha sostituito lo switcher orizzontale con una vista impilata senza cap, un componente `CustomerOrderPanel` isolato per ordine (nessuno stato SumUp/pagamento condiviso), CTA inline per ordine (niente più fixed bottom bar globale) e reconciliation SumUp scoped al solo ordine target in `?orderId=`. Non ha richiesto DB/migration/RLS (confermato in audit). Nessun file Payment Hub/DB/RLS/KitchenPrepBoard/routing toccato. Lavoro di questo e del sprint precedente (`CustomerOrderStatus.jsx`, nuovo `CustomerOrderPanel.jsx`, `CustomerKitchenMenu.jsx`, `tests/e2e/customer-kitchen-flow.spec.js`) **NON committato, NON pushato** — decisione di Eros se/quando committare. Il lifecycle cliente descritto nel checkpoint `docs/checkpoints/WALBOX_KITCHEN_PAYMENT_CUSTOMER_LIFECYCLE_CHECKPOINT_2026-09-18.md` è ora coperto interamente (AC1-AC7).

**Kitchen — RILASCIATA su `main` il 2026-09-16, go-live serata 18/09.** `main` e `integration/fantawalrus-kitchen-v1` sono allineati sullo stesso HEAD; Vercel Production Branch = `main`, deploy verificato in produzione (smoke `/kitchen`, `/kitchen/status`, `/kitchen/cassa`, `/kitchen/solo`, sezioni Taglieri/Birre/Bevande/Box, asset, auth guard, privacy browser pulito). **Non riaprire il track Kitchen senza una richiesta esplicita di Eros.** Restano aperte, non bloccanti: BEER SPRINT V1 Fase F (analytics, rimandata per decisione Eros), i 2 fail di test pre-esistenti a registro in OPEN ISSUES, e l'hardening della lista ordini del locale che arriva **in RAM** quando un browser ha una sessione staff attiva (mai persistita, mai renderizzata — richiederebbe una query cliente-scoped dedicata, proposta separata).

**Kitchen — BEER SPRINT V1 (2026-09-14, `integration/fantawalrus-kitchen-v1`): Fase A+B+C+D+E chiuse in locale, NON committate, NON pushate.** Vedi DONE per dettaglio dati/UI/QA/test. **Prossima fase della missione** (`ai-ops/current/BEER_SPRINT_V1.md` §7): Fase F (analytics minimi `beer_view`/`beer_expand`/`beer_add`/`beer_choice_helper_click`/`beer_pairing_impression`/`beer_pairing_accept` — rimandata per decisione Eros, repo oggi a zero tracking). **Fase C conclusa come audit-only** (STOP confermato sulla parte asset: nessun asset reale trovato per le 7 birre, nessuna immagine fake creata; il sistema di fallback in `BirreSection.jsx`/`.css` è già coerente e pronto ad accogliere foto reali in futuro). **Fase D conclusa**: matrice `kitchenBeerPairing` + banner contestuale nel drawer carrello, un solo suggerimento per contesto. **Fase E conclusa in due parti**: (1) Krombacher €6 confermato, ordinabile la sera (gate 18:00 già esistente, ora regola definitiva, estratto in `src/lib/kitchenServiceRules.js` come unica fonte); (2) su decisione esplicita di Eros, FALLO PESANTE ora richiede la scelta di 1 birra inclusa tra le 6 bottiglie + Krombacher (dalle 18:00), **prezzo del combo invariato** — vedi DONE per il razionale tecnico completo (id composito solo UI, `itemId` reale invariato per non rompere l'allowlist promo). Due nuovi test E2E (20, 21) verdi, zero regressioni misurate single-worker su 3 suite. **Decisioni aperte per Eros:** (a) formato in cl standalone Krombacher — ancora non confermato, non inventato, oggi assente dalla card (solo `choiceLabel: 'ALLA SPINA'`); (b) i pairing food→birra di Fase D restano un'ipotesi prodotto iniziale (§5 missione, matrice in `kitchenMockData.js`), da validare/correggere con Eros — Krombacher deliberatamente non aggiunta ai pairing, e FALLO PESANTE (che ora include sempre una birra) non è stato escluso dal banner "CI STA BENE UNA BIRRA?" perché quel banner è un upsell per una birra aggiuntiva, non un rilevamento "manca la birra" — da confermare se questo comportamento è quello voluto; (c) asset reali birre quando disponibili (Fase C, oggi bloccata solo per mancanza foto affidabili); (d) analytics Fase F, quando Eros la vuole attivare; (e) commit dei file modificati (2 iniziali + 2 nuovi da Fase A+B, 3 modificati da Fase D, ora 3 modificati + 2 nuovi file da Fase E — nessun commit fatto in questa run, solo working tree). Nessun push/deploy/DB in questa fase.

**Kitchen — Promo Pass Redemption V2, Opzione 2 customer self-redeem (2026-09-10): schema/RPC live sul remoto e verificati con smoke reale (vedi DONE), commit locale dedicato appena creato in questa run.** Nessuna decisione aperta per Eros su questo punto: redemption cliente verificata live (owner/foreign/staff/idempotenza/double-use/Payment Hub tutti PASS), cleanup smoke completato, commit fatto, nessun push. Prossimo eventuale step: autorizzare il push del branch quando Eros lo riterrà opportuno (branch resta comunque bloccato dal gate di certificazione runtime multi-device, vedi track Kitchen sotto).

**Kitchen — Staff Auth Harness standardizzato (2026-09-09, `integration/fantawalrus-kitchen-v1`, sopra `af48f89`): config split F-SEC-2 risolto.** `playwright.config.js` (suite principale) esclude via `testIgnore` `tests/e2e/f-sec-2-kitchen-guard.spec.js` — non esegue mai il guard reale (inietta `VITE_E2E_BYPASS_STAFF_AUTH=true`, per design). `playwright.fsec2.config.js` è l'unico harness F-SEC-2 dedicato (`testMatch` limitato allo stesso spec, porta 5194, nessun bypass): verificato in questa sessione, **3 passed (T1, P1, P2) / 3 skipped (T2, T3, T4, motivo esplicito storageState assente)**. Account test canonico: **`staff87`** (`kitchen_staff_members`, `venue_id='walrus-main'`, già esistente, nessun nuovo utente da creare). `FSEC2_STORAGE_STATE` è locale/non committato (`tests/e2e/.auth/`, aggiunto a `.gitignore`), mai credenziali o sessioni nel repo. Contratto completo: `docs/sprint3b-f-sec-2-storage-state-contract.md`. **Prossimo gate reale:** generare `FSEC2_STORAGE_STATE` (login manuale `staff87` via `npx playwright codegen --save-storage=...`, vedi doc) e completare T2-T4 con auth reali. Questo gate è indipendente e non blocca né sostituisce i gate Kitchen aperti sotto.

**Track attivo Kitchen (2026-09-09): STAFF UX CONSOLIDATION chiusa e committata su `integration/fantawalrus-kitchen-v1`, HEAD `9f8b825`. `CODE_READY_FOR_RUNTIME_CERTIFICATION: YES`, `SPRINT3B_FINAL_CERTIFIED: NO`.** Il working tree non ha più WIP Kitchen in sospeso: staff consolidation (`/kitchen/solo` unica UI, `/kitchen/staff` compat redirect, legacy TEAM rimosso, entry redirect race fix), audio, migration ledger reconciliation, test Realtime, label Payments e test riallineati sono tutti committati in locale (vedi DONE). `npm run build` PASS, `npm run test:unit` 272/272 PASS su HEAD corrente. **Gate ancora aperti, in ordine:** (1) **certificazione runtime multi-device reale** — staff e cliente concorrenti su Supabase reale, non eseguibile in sandbox, richiede Eros da terminale reale; è il gate che separa `CODE_READY` da `SPRINT3B_FINAL_CERTIFIED`; (2) **push del branch**, mai eseguito (`git push -u origin integration/fantawalrus-kitchen-v1`, da autorizzare); (3) **F-SEC-2 fuori dal config principale**: i 2 test del guard staff (T1, P1) falliscono sempre sotto `playwright.config.js` finché resta `VITE_E2E_BYPASS_STAFF_AUTH=true` con lo spec nella stessa `testDir` — verde sotto `playwright.fsec2.config.js` dedicato; vanno esclusi dal config principale o spostati, decisione da prendere (baseline nota, invariata da questa sessione); (4) **bridge Jukebox `?table=` con fallback hardcoded `'7'`** — cross-domain, decisione di prodotto separata, invariato. Migration ledger: **RISOLTO e committato** in `ba6dd7a` (vedi DONE), non più un gate aperto. **Nota di copertura:** la label `card_counter_manual` non ha una fixture demo che la renderizzi (`kitchenStaffPaymentsDemoFixtures.js` non contiene pagamenti con quel metodo); la chiave è però verificata end-to-end lato scrittura, perché il test riallineato asserisce che `CARTA/POS ✓` chiama la RPC con `p_method: 'card_counter_manual'`. **Superato lo stato precedente (2026-09-06):** Codice e suite automatizzata chiusi (vedi DONE): cleanup no-tables, pagamento banco allineato, 5 test E2E rossi riallineati al contratto attuale, 17/17 + 26/26 + 5/5 + 2/2 PASS, build PASS. **"CODE_READY" non è "certificato":** il gate successivo è la **certificazione runtime multi-device reale** — staff e cliente concorrenti su Supabase reale, oggi non eseguibile in questo sandbox (redazione `VITE_SUPABASE_URL`/`ANON_KEY`), richiede Eros da terminale reale con l'ambiente vero. Resta OPEN e volutamente non toccato in questa baseline (vedi OPEN ISSUES) il bridge Jukebox `?table=` con fallback hardcoded `'7'` (cross-domain, decisione di prodotto separata). **Decisioni che servono a Eros:** (a) eseguire/autorizzare la certificazione runtime multi-device da terminale reale; (b) quando aprire il bridge Jukebox `?table='7'`; (c) se committare la riconciliazione del ledger del 2026-09-07 (vedi DONE) insieme al push del branch. Nessun commit/push/deploy/DB eseguito oltre al commit locale di questa baseline (vedi DONE). Report: `ai-ops/reports/sprint3b-closure-run-fase-ab.md`, `ai-ops/reports/sprint3b-closure-run2-test-alignment.md`.

**Track precedente nella stessa area (2026-09-04, storico): Customer Kitchen su `integration/fantawalrus-kitchen-v1` — verdetto FINAL_VISUAL_READY.** HEAD locale `87bd41f`, 7 commit non pushati sopra `4b30172` (`be7c031`, `a537b3a`, `53cbfb4`, `d0df13e`, `c697700`, `bc3888e`, `87bd41f`). Zero P0/P1 visuali aperti, zero regressioni su build/unit/E2E. **FINAL_VISUAL_READY è un verdetto visuale, non un "pronto ad aprire ai clienti":** restano tre gap che sono input di Eros e non codice — (1) **17 voci su 20 senza prezzo**, quindi non ordinabili (Panini 8/8, Cicchetti 4/4, Insalatone 3/3, Tartare 2/2; solo i 3 Pesi Massimi prezzati); (2) **le 3 voci ordinabili non dichiarano allergeni** (`allergens: []` su `item-009/010/011`, più `item-013` e `item-021`), che è conformità Reg. UE 1169/2011 prima che prodotto — nessun dato inventato; (3) **nomi segnaposto "TARTARE 1 / 2"** che arrivano al cliente e alla comanda. Non bloccanti ma aperti: direzione fotografica da unificare (5 mondi diversi nella stessa schermata; §5.2 della skill fissa la reference solo per i Cicchetti, che sono l'unica famiglia conforme); **8 PNG orfani, 14,41 MB**, senza riferimenti in `src/` ma copiati verbatim in `dist/` — fra cui `panino_mortadella.png`, che ha una banda nera da 57px sul bordo destro e non è raggiungibile dal cliente (rimozione = `rm`, serve approvazione); Figma `164:2` **stale** (mostra ancora Cicchetto 3 impiattato con la label cotta: va aggiornato il Figma, non il codice); carrello perso al reload (è persistenza, non fix visivo); `.gitignore` per `.playwright-mcp/`, `kitchen-home-check.png`, i 6 `qa-0*.png` e `a2-390-home.png`. **Decisioni che servono a Eros: (a) prezzi, (b) allergeni, (c) nomi Tartare, (d) direzione fotografica, (e) destino dei PNG orfani, (f) autorizzare il push.** Comando push proposto e **non eseguito**: `git push -u origin integration/fantawalrus-kitchen-v1`. Il WIP Sprint3B/fulfillment/realtime resta dirty e non incluso: nessuno dei 7 commit ne dipende (build PASS su worktree pulito), e comunque il push riguarda solo i commit.

**Stato precedente della stessa run (storico): Customer Kitchen — verdetto PRE_PUSH_BLOCKED.** HEAD locale `09ea248`, 9 commit non pushati (`ba41438..09ea248`). Blocker 1 (asset watermark) risolto; blocker 2 ridotto da 4 test a 1, classificato `ENV_INTEGRATION_REQUIRED`; **è emerso il blocker 3, un dead-end customer reale in `1f327e0`** (vedi OPEN ISSUES). Decisioni che servono a Eros: (1) **autorizzare il fix del blocker 3** — è il solo ostacolo tecnico rimasto al push, tocca `CustomerKitchenMenu.jsx` che è in STABLE, fix proposto già scritto; (2) decidere sui rilievi should-fix della fresh review, in ordine di impatto: allergeni invisibili in tutte e 5 le categorie navigabili + chiave `frutta_secca` mancante in `ALLERGEN_LABEL`, guard `noPrice` assente in `PesiMassimiSection`, assenza totale di test sulla back navigation, separatore decimale incoerente; (3) prezzi degli 8 panini (categoria oggi non ordinabile), allergeni `item-007`, conferma del nome `Fallo Combo`; (4) rimuovere o tenere il file watermark ora orfano, che Vite copia comunque in `dist/`; (5) destino del WIP Sprint3B/fulfillment/realtime lasciato dirty (8 file modificati + untracked `src/lib/kitchenOrder*.js`, `src/hooks/useKitchenAudio.js`, 5 migration, `tests/unit/sprint3b-kitchen-realtime.test.mjs`) — il push riguarda solo i commit, quindi il WIP non partirebbe comunque; (6) autorizzare il push. Da valutare anche `.gitignore` per `.playwright-mcp/`, `kitchen-home-check.png` e i 6 screenshot `qa-0*.png` della QA di questa sessione (artefatti untracked, nessuno necessario). Comando push proposto e **non eseguito**: `git push -u origin integration/fantawalrus-kitchen-v1`. **Nessun push, deploy o mutazione DB/env/Auth eseguiti.**

Track precedente, in pausa: **FantaWalrus**. Stato appena verificato (2026-08-25): `npm run test:unit` 272/272 PASS, `npm run build` PASS, working tree con WIP Figma rebuild F5/F6 non committato (vedi DONE sopra). NEXT STEP reale: **Extraction Audit** su FantaWalrus (da definire scope con Eros) prima di proseguire F5/F6 del rebuild UI. Restano sullo sfondo, non attivi: ai-factory-runner (V1.6.1, già pushato, nessun next step aperto), Jukebox/Shuffle Night (PILOT_NIGHT_CHECKLIST S3/S4, stash `CustomerEntry.jsx`/`CustomerRequest.jsx`). Kitchen — Solo Service Mode V2 (2026-08-27): finalizzato e committato in locale, non pushato; nessun next step aperto salvo eventuale verifica `playwright.fsec2.config.js` con env Supabase reali (gap noto, non bloccante). Kitchen — P0-A Write Reliability (2026-08-27): RESOLVED, committato in locale, non pushato; nessun next step aperto. Kitchen — Solo Live Demo Harness `/kitchen/solo-demo` (2026-08-27): finalizzato e committato in locale, non pushato; nessun next step aperto. Kitchen — P0-B Undo Window (2026-08-27): PASS, finestra 9s, committato in locale, non pushato; nessun next step aperto salvo eventuale P0-C se richiesto esplicitamente da Eros. Kitchen — Payment Hub V1 SumUp sandbox (2026-08-28): E2E reale PASS, fix lifecycle `kitchen_payment_confirm` applicato al remoto e riverificato; nessun next step aperto salvo eventuale riconciliazione del migration ledger (vedi OPEN ISSUES) se Eros vuole tornare a poter usare `supabase db push` su questo progetto.

## RESTART PROMPT
"Walbox — Kitchen stabile e completa (V1-P6). Track attivo: Jukebox/Spotify reale per Shuffle Night (auto-advance, TV sync, ricerca), 39 commit dal 24/6 al 3/7. Prossimo step: walbox-dev fa piano da PILOT_NIGHT_CHECKLIST.md per chiudere fino a demo-stabile."
